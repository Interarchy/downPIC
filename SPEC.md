# AI 建筑灵感素材库｜MVP SPEC v1.0

> 文档状态：需求确认完成  
> 目标平台：Windows / Chrome  
> 产品阶段：邀请制 MVP 测试

## 1. Problem Statement（问题陈述）

### 1.1 目标用户

建筑、室内和景观设计师，主要处于方案前期的概念设计和案例研究阶段。

### 1.2 当前工作方式

设计师在 gooood、小红书、Pinterest 等网站寻找参考图，通过浏览器逐张执行：

`右键 → 图片另存为 → 选择目录 → 手动命名`

一次典型任务需要保存约 30～100 张图片。

### 1.3 核心问题

现有工作流存在四个连续断点：

1. 采集操作重复，单张图片需要多次点击和目录选择。
2. 下载后的图片缺少结构化标签，依赖文件名和人工记忆。
3. 素材分散在不同项目文件夹中，难以长期积累和复用。
4. 用户记得图片的空间感受或设计特征，却无法通过自然语言找回。

市场上的普通下载工具只解决“保存文件”，传统素材管理工具仍高度依赖人工分类，没有形成以下完整闭环：

**主动采集 → AI 理解 → 人工校正 → 自然语言检索 → 项目复用**

### 1.4 产品假设

如果设计师能够用一次点击保存目标图片，并通过 AI 标签与自然语言重新找到历史素材，他们会持续建立和复用个人素材库，而不再把产品当作一次性下载工具。

## 2. Proposed Solution（方案描述）

### 2.1 产品形态

产品由两个部分组成：

| 组件 | 主要职责 |
| --- | --- |
| Chrome 浏览器插件 | 项目类型选择、网页标题项目名、网页图片选择、原图获取和采集反馈 |
| Windows 桌面素材管理应用 | 图库、文件夹、未解析、AI 分析、关键词校正、项目组织、混合搜索和本地后台任务 |

第一版面向 Windows，仅支持 Chrome。

### 2.2 核心用户流程

1. 用户通过邀请码注册基础账号。
2. 启动 Windows 桌面素材管理应用，系统创建默认素材目录并与插件建立连接。
3. 用户在插件中选择第一层项目类型；类型可来自预设词库或用户自定义。
4. 用户开启插件的“图片保存功能”，鼠标经过一张可保存图片，在悬浮工具条中确认或调整项目类型后点击“保存”。
5. 插件读取网页标题，自动生成可编辑的第二层项目名称。
6. 系统建立“项目类型 / 项目名称”两级文件夹，并获取当前权限范围内可访问的最高分辨率图片。
7. 图片保存到该项目文件夹，同时进入全局素材库和“未解析”视图。
8. 图片自动进入 AI 解析队列；临时失败自动重试 2 次，仍失败后保留人工重试入口。
9. AI 生成图片描述、关键词标签和视觉语义信息，结果立即参与搜索与筛选。
10. 用户需要时修正描述或关键词标签，修改自动保存并覆盖对应 AI 内容。
11. 用户通过统一搜索框，用自然语言、关键词或项目类型查找素材。
12. 用户打开、复制、拖拽图片，或返回原始网页。

### 2.3 图片采集

第一版只支持用户主动逐张点击，不扫描和批量下载整个页面。

功能要求：

- 插件包含两个极简交互面：浏览器右上角弹窗与网页图片悬浮工具条。
- 右上角弹窗只保留三项可操作能力：图片保存功能开关、默认项目类型选择/自定义输入、打开桌面应用。
- 仅在图片保存功能开启且鼠标经过可保存的内容图片时显示悬浮工具条；图标、头像、按钮素材等非内容图片不显示。
- 悬浮工具条始终并列显示预设类型选择、自定义类型输入和“Save”，不把自定义入口收纳在下拉框内。
- 右上角弹窗始终显示预设类型选择、自定义类型输入和“设为默认”；弹窗不使用“Save”，因为没有当前图片作为保存对象。
- 预设选择与自定义输入互斥：开始输入自定义类型时取消预设选择，重新选择预设类型时清空自定义输入。
- 自定义项目类型提交前必须去除首尾空白；空字符串、纯空格、纯换行或制表符均视为无效，不能保存图片或创建类型。
- 插件记住用户最近一次有效项目类型，并作为弹窗和所有悬浮工具条共享的默认值。
- 用户在悬浮工具条中点击“Save”后，所选类型才同时用于当前图片并写回全局默认值；弹窗点击“设为默认”只更新后续默认类型。
- 有效自定义类型提交后进入“我的类型”，后续可直接从项目类型下拉框选择。
- 未选择有效项目类型时，不能开启图片保存功能。
- 桌面应用连接状态只以轻量状态点呈现；窗口关闭但后台服务运行时仍可正常保存。
- 桌面端完全退出时，插件进入离线暂存模式：图片下载到浏览器默认下载目录下的固定待导入目录，并保存项目与来源元数据；下次启动桌面端后自动导入、归档和解析。
- 保存成功、重复和失败只使用临时反馈，不在插件内保留历史列表；成功或重复反馈包含最终“项目类型 / 项目名称”和“打开所在文件夹”。
- 正式归档确认后清除 Chrome 对应的临时下载历史项，避免移动后的暂存文件被误解为素材已删除。
- Logo 弹窗提供“打开素材文件夹”，直接打开包含各一级项目类型目录的素材库根目录；插件内不展示文件浏览器。
- 网页标题项目名、AI 额度、图库、AI 分析、关键词、搜索、账号和设置均不在插件中管理。
- 开启图片保存功能后，网页图片本身保持原有交互；只有点击悬浮工具条中的“保存”才触发保存。
- 灯箱或模态画廊打开时，悬浮工具条只绑定最上层画廊中的当前大图；点击 Save 后目标保持锁定，背景图片不能造成浮框跳动或目标切换。
- 采集前只要求用户选择第一层项目类型，不要求手动填写项目名称。
- 项目类型来自插件预设词库，并允许用户新增自定义类型。
- 首次点击当前网页图片悬浮工具条中的“保存”时，读取网页标题并自动生成第二层项目名称。
- 用户可以在桌面应用中修改自动生成的项目名称。
- 同一网页后续采集的图片继续进入同一项目文件夹。
- 网页标题无效时，使用“网站名称＋采集日期”作为备用项目名称。
- 文件夹名称必须清理 Windows 非法字符；同名但来源 URL 不同的项目使用网站名或编号避免冲突。
- 优先获取可访问的最高分辨率版本。
- 原图不可获取时，回退到最佳可用版本。
- 支持 JPEG、PNG、WebP。
- 通过文件内容识别完全重复文件并阻止重复保存。
- 不进行视觉相似检测。
- 显示保存成功、重复文件或失败原因。
- 悬浮工具条支持就绪、自定义输入、保存中、保存成功、已离线暂存、当前项目内重复、失败和格式不支持状态；反馈应轻量且不遮挡图片主体。
- 通用网页提供基础采集能力。
- 重点适配 gooood、小红书和 Pinterest。
- 只处理用户在正常登录和访问权限内可见的内容。

每张图片记录：

- 原始图片 URL
- 来源页面 URL
- 原始网页标题
- 网站名称
- 采集时间
- 项目类型
- 项目名称

### 2.4 本地素材管理

网页采集图片按“素材根目录 / 项目类型 / 项目名称 / 图片”存储。

目录策略：

- 首次启动由用户选择一个素材库根目录，并完成读取、写入和建目录权限检查。
- 首次设置提供创建新素材库、连接已有素材库、移动现有素材库三种模式；确认前展示两级物理目录预览。
- 权限检查失败时禁止完成设置，显示可操作原因并允许重新选择；正式实现不得请求管理员权限来绕过受保护目录。
- 设置中分别提供“移动现有素材库”和“连接另一个素材库”。
- 移动现有素材库时由产品完成文件迁移与索引更新；失败时回滚到原目录。
- 用户切换项目类型后，后续网页项目进入新的第一层目录。
- 插件中人工选择的项目类型是物理存储依据，AI 分析不得自动修改或移动目录。
- 修改项目类型时移动整个二级项目文件夹。
- 修改项目名称时同步重命名二级文件夹。
- 同一项目文件夹内对完全重复文件去重。
- 同一图片属于不同项目时，在各项目文件夹保存独立副本。
- 用户可导入单张或多张本地图片。
- 用户可绑定本地文件夹，持续发现新增或删除内容。
- 绑定文件夹中的图片导入后生成产品管理副本。
- 目标容量为每位用户约 5,000 张图片。

删除图片时：

- 操作名称必须明确显示为“移到回收站”。
- 本地图片进入系统回收站，同时移除素材库索引和关联关系。
- 完成后提供短时间撤销；失败时保留原文件和素材库记录。

第一版不提供备份和恢复能力，需在产品中明确提示本地数据风险。

### 2.5 素材组织

采用物理目录与逻辑视图结合的结构：

- 项目类型：第一层物理目录，由插件预设或用户自定义。
- 项目名称：第二层物理目录，默认由网页标题自动生成并允许修改。
- 全局素材库：跨目录索引全部图片。
- 未解析：跨目录显示所有尚未完成 AI 解析的图片。
- 收藏夹：用于主题整理和灵感策展，不额外复制文件。

同一项目内不保存完全重复文件；不同项目之间允许保存独立副本。收藏夹仅建立逻辑引用。

### 2.6 关键词标签体系

第一版的关键词标签按建筑专业维度组织：

- 空间
- 材料
- 构造
- 形态
- 风格
- 氛围
- 其他

关键词标签规则：

- 上线前由 AI 一次性生成基础词库。
- 产品方人工审核后内置。
- AI 优先从当前有效预设词库中选择关键词标签；预设不足时每张图片最多自主新增 5 个标签。
- AI 新增标签直接关联当前图片，进入“其他”分类和个人词库，用户可以删除、改名或移动分类。
- AI 生成的关键词标签立即参与搜索和筛选，并标明 AI 来源。
- AI 可以提出不同的“项目类型”关键词，但只作为建议；只有用户明确执行项目类型变更时才移动项目文件夹。
- 用户可以重命名、增加、删除个人预设标签。
- 用户修改只影响自己的素材库。
- 用户可以创建自定义标签。
- 自定义标签创建后立即进入 AI 候选范围。
- 标签名称必填，文字定义和参考图片可选。
- 删除已使用标签时采用归档机制。
- 归档标签保留在历史图片中，但不再参与新图片分析。
- 重命名标签时同步更新所有关联图片。

关键词标签来源包括：

- AI 生成
- 用户创建
- 用户修改

AI 关键词标签立即参与搜索，但界面必须标明其来源。用户修改后的内容优先于 AI 结果，普通重新解析不得覆盖人工修改。

### 2.7 AI 分析

AI 由产品内置的云端服务提供，用户不需要配置 API Key。

AI 输出包括：

- 图片语义描述
- 预设或自定义关键词标签
- 图文检索向量

触发方式：

- 新保存或新导入图片自动进入解析队列。
- 临时失败自动重试 2 次，仍失败后进入“解析失败”。
- 用户可以单张或批量重试失败项，也可以主动重新解析已解析图片。
- 显示队列总进度、单项状态和可理解的失败原因。

测试期免费但设置调用上限，同时记录模型成本、响应时间和失败率。第一版不建立正式收费体系。

### 2.8 搜索体验

首页结构：

- 顶部突出统一搜索框，应用启动后自动聚焦。
- 输入时显示历史搜索和关键词建议，按 Enter 执行自然语言搜索。
- 主体依次显示最近添加、未解析进度摘要和最近项目。
- 一级导航包含“未解析”，并显示未解析图片数量。
- 图片卡片直接显示已有关键词标签和主要匹配原因。
- 关键词标签需区分 AI 与人工来源。
- 图片详情将关键词区收敛为“当前关键词”和“添加关键词”两层；添加区使用单一输入框同时完成已有词筛选与新词创建。候选由全素材库实时聚合、排除当前图片已有关联，并以紧凑数字角标显示关联图片数。
- AI 标签使用冷绿色系，人工标签使用暖色系，不在每个标签后追加“AI/人工”文字；无障碍名称和数据字段仍保留来源。
- 图片详情中的标签正文为只读展示区，只有独立的“×”按钮触发删除；每张图片维护独立编辑历史栈，最多保存 50 个描述或标签修改快照，允许连续撤销。
- 图片详情底部展示来源网站、原始网页标题、来源页面 URL、采集时间和打开来源页面入口；字段缺失时明确标注，不以推测值替代。
- 详情关闭按钮与头图右上角对齐；头图提供“查看原图”，打开覆盖应用窗口的原图查看器。查看器支持独立关闭按钮和 Esc，关闭后回到原详情并恢复触发按钮焦点。
- 左侧栏包含两组同等重要的浏览入口：
  - 按项目浏览：按照“项目类型 / 项目名称”物理文件夹层级查看图片。
  - 按关键词浏览：按照空间、材料、构造、形态、风格、氛围和其他分组查看图片。
- 关键词栏支持搜索关键词、展开分类并显示每个关键词关联的图片数量。
- 项目范围与多个关键词标签之间采用 AND。
- 提交新的非空自然语言查询时，清除提交前已经选择的项目与关键词，让自然语言先检索整个素材库，并显示清除反馈。自然语言结果出现后再选择的项目或关键词属于用户主动精筛，才与查询采用 AND。
- 当前生效的项目范围、关键词和查询理解必须以可清除条件显示在内容区顶部。
- 未选择任何左侧范围时，顶部自然语言搜索默认检索整个素材库。

统一搜索框同时支持：

- 自然语言查询
- 关键词匹配
- 系统预设关键词
- 用户自定义关键词
- AI 图片描述
- 图文语义向量

自然语言是主入口。系统需显示对查询的理解，将识别出的条件转换成可编辑标签或条件，用户可删除、增加或修改，避免黑箱搜索。

左侧项目浏览回答“我的图片来自哪些项目和文件夹”，左侧关键词浏览回答“具有某种空间、风格或材料特征的图片有哪些”，顶部自然语言搜索回答“哪些图片符合这段设计意图”。三种入口共享同一结果区。

断网时：

- 可以浏览本地图库。
- 可以编辑标签和组织关系。
- 可以使用标签及关键词搜索。
- 无法执行 AI 图片分析和自然语言混合搜索。

### 2.9 未解析视图

所有新保存和新导入图片默认进入“未解析”视图并自动排队解析。

解析状态包括：

- 未解析
- 解析中
- 已解析
- 解析失败

只有同时生成图片描述和至少一个关键词标签，才视为解析成功并离开“未解析”视图。失败或结果不完整的图片继续保留；临时失败自动重试 2 次，之后提供单张或批量人工重试。

数量口径统一为：导航“未解析”角标 = 未解析 + 解析中 + 解析失败。首页队列、未解析页总数和状态筛选均从同一状态集合实时计算；已解析数只用于整体进度，不计入角标。

“未解析”是跨项目的逻辑视图，不移动或复制磁盘文件。

### 2.10 素材复用

搜索结果支持：

- 打开大图
- 查看来源网页
- 在 Windows 文件夹中定位原文件
- 复制图片
- 拖拽到其他设计软件

第一版不提供项目、收藏夹或来源清单导出。

### 2.11 账号与测试机制

- 第一版采用邀请码测试。
- 用户需要注册基础账号。
- 账号用于邀请资格、AI 免费额度及测试统计。
- 账号不承载用户图片和桌面端本地素材库。
- 第一版不支持换机恢复或跨设备同步。

### 2.12 数据与隐私

私人素材与未来可能建设的公共素材库严格分离。

- 用户原图保存在本地。
- 图片在自动解析任务开始时上传；设置中必须明确说明该行为并允许暂停自动解析。
- AI 处理完成后应删除云端临时图片。
- 不得默认将用户图片用于训练、公共素材库或其他用途。
- AI 服务商需要支持相应的数据删除或零留存机制。
- 图片内容不得进入产品行为分析系统。

测试阶段拟收集：

- 采集行为
- AI 分析行为
- 关键词标签的人工修改
- 搜索词
- 搜索路径
- 结果浏览与复用动作
- 错误日志

详细行为数据保存 30 天，之后删除或不可逆匿名化。

注册时必须单独说明详细数据的种类、目的、保存期限和处理方，并获得明确同意；拒绝或撤回不能影响核心功能。用户应能关闭统计并删除历史数据。

## 3. Technical Constraints（技术约束）

### 3.1 平台限制

- 第一版仅支持 Windows。
- 仅支持 Chrome。
- 桌面端必须以独立应用窗口和应用图标呈现，不要求用户通过浏览器打开管理界面。
- 文件保存、数据库、索引、文件夹监听和插件通信由桌面应用内部的本地核心模块负责。
- 浏览器插件与桌面应用必须提供清晰、可信的连接状态。
- 桌面应用负责启动和管理内部本地核心模块，用户不需要单独安装或启动第二个本地服务。
- 安装流程应尽量做到桌面应用一键安装、插件自动发现和连接。
- 桌面窗口关闭且仍有 AI 或目录监听任务时，必须明确提示继续后台运行或完全退出。

### 3.2 数据架构

- 图片、标签、项目关系和搜索索引以本地数据为准。
- 本地数据库需支持至少 5,000 张图片。
- 项目类型和项目名称映射到两级物理文件夹。
- 不同项目允许保存独立图片副本；收藏夹使用引用关系。
- 图片必须保存解析状态、描述和关键词标签。
- 必须保存关键词标签来源、人工修改状态和词库版本。
- 必须为未来同步预留稳定的图片与实体 ID，但第一版不实现同步。

### 3.3 AI 架构

- 模型供应商尚不固定。
- 图片理解、文本向量和搜索能力必须通过可替换的模型接口提供。
- 更换模型时不能破坏已有标签和本地索引。
- 真实测试前必须完成 AI 服务的数据留存、数据接收方和可能的数据出境评估。

### 3.4 网络与离线

- 本地浏览、管理、关键词标签编辑和筛选不能依赖云端。
- AI 分析和自然语言查询依赖网络。
- 网络错误不能导致图片或人工修改丢失。
- 失败任务必须可见且可重试。
- 桌面端完全退出不阻止插件下载；但插件必须明确反馈“已暂存，打开桌面端后自动整理”，不能假装已完成正式归档或 AI 解析。

### 3.5 网站与版权

- 不绕过登录、付费墙、防盗链、验证码或访问控制。
- 网站结构变化可能导致专项适配失效，需要监控和维护。
- 产品只提供采集与管理能力，不向用户授予图片版权。
- 首次使用时必须显示版权与合法使用提示。
- 保存来源信息不能替代用户取得合法使用授权。

## 4. Non-goals（明确不做的事）

第一版不做：

- 整页图片扫描、批量勾选或自动批量下载
- macOS、Edge、Firefox、Safari 和移动端
- 用户云端图库和跨设备同步
- 团队空间、多人权限、评论和协作
- 公共素材资源库
- 以图搜图
- 视觉相似检测和自动归组
- GIF、SVG、AVIF、视频和动图
- AI 对项目类型或物理文件夹的自动改动
- 本地 AI 模型
- 正式订阅、点数充值和计费系统
- 项目或收藏夹导出
- 本地备份和恢复
- 绕过网站技术或权限限制
- 材料节点和施工构造知识库
- 汇报排版或 moodboard 制作工具

## 5. Success Criteria（成功标准）

### 5.1 北极星指标

**10 日有效复用率 ≥40%**

统计条件：

- 至少 30 名已激活测试用户。
- 用户首次使用 3 天内采集或导入至少 10 张图片。
- 至少完成 5 张图片的 AI 分析。
- 至少成功使用一次搜索。
- 用户在第 8～10 天通过搜索或标签重新找到图片，并发生打开大图、返回来源或加入项目等行为，视为有效复用。
- 仅打开产品或继续下载图片不算有效复用。

### 5.2 搜索质量

采用真实任务测试为主、线上行为为辅：

- 20 名用户，每人完成 5 个真实找图任务。
- 用户在两次查询以内找到满意图片。
- 满意图片出现在前 10 个结果中。
- 完成时间不超过 60 秒。
- 任务成功率达到 80%。

点击、停留、查询改写等行为仅作为诊断信号，不单独定义搜索成功。

### 5.3 AI 标签质量

- AI 关键词标签保留率 ≥80%。
- 至少 80% 的图片覆盖六个主要关键词标签维度中的三个。
- 系统预设标签与用户自定义标签分别统计。
- 标记为“已解析”的图片必须 100% 同时包含图片描述和至少一个关键词。
- 解析失败或结果不完整的图片必须继续出现在“未解析”视图。

### 5.4 采集效率

- 选择当前项目类型后，项目名称自动生成，单张图片只需一次点击。
- 采集 30 张目标图片所需时间，相比浏览器原生“另存为”流程降低 ≥70%。

### 5.5 采集可靠性

- gooood、小红书、Pinterest 分别统计。
- 在用户正常可见且有权访问的支持格式图片中，采集成功率 ≥90%。
- 登录失效、内容删除和网站明确阻止访问的情况不计入有效样本。

### 5.6 性能

在约 5,000 张图片的普通 Windows 测试环境中：

- 图库首屏 3 秒内可用。
- 关键词标签搜索 2 秒内返回。
- 自然语言混合搜索 5 秒内返回。
