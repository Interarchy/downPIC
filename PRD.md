# AI 建筑灵感素材库｜MVP PRD v1.0

> 文档类型：产品需求文档（PRD）  
> 文档状态：待产品评审  
> 需求基线：[SPEC.md](./SPEC.md)  
> 目标平台：Windows / Chrome  
> 发布方式：邀请码制 MVP 测试  
> 目标用户：建筑、室内、景观设计师

## 0. 文档说明

### 0.1 文档目标

本 PRD 将已确认的产品 SPEC 转化为可用于产品设计、原型验证、技术评审、开发拆解和测试验收的执行文档。

### 0.2 版本记录

| 版本 | 状态 | 说明 |
| --- | --- | --- |
| v1.0 | 待评审 | 基于 SPEC v1.0 整理 MVP 完整需求 |

### 0.3 优先级定义

| 优先级 | 定义 |
| --- | --- |
| P0 | 缺失后无法完成核心闭环或无法开展测试，必须进入 MVP |
| P1 | 明显提升体验或稳定性；可在不影响核心测试时延后 |
| P2 | 后续版本候选，不进入本次 MVP |

## 1. 产品概述

### 1.1 一句话定义

一款面向建筑、室内和景观设计师的网页图片采集与 AI 素材管理产品，让设计师能够主动点击保存参考图、使用 AI 完成结构化标注，并通过自然语言重新找到历史素材。

### 1.2 产品愿景

把设计师分散、依赖记忆的参考图文件夹，转化为可理解、可校正、可检索、可持续积累的个人设计知识库。

### 1.3 核心价值闭环

**主动采集 → 本地沉淀 → AI 理解 → 人工校正 → 混合检索 → 项目复用**

产品价值不以“成功下载一次”衡量，而以用户是否持续积累并重新使用历史素材衡量。

### 1.4 产品形态

产品由三个部分组成：

| 组件 | 职责 |
| --- | --- |
| Chrome 浏览器插件 | 选择项目类型、读取网页标题、主动单图采集和反馈 |
| Windows 桌面素材管理应用 | 文件夹、图库、未解析、AI 任务、关键词、搜索和后台监听 |
| 云端账号与 AI 服务 | 邀请登录、AI 额度、图片理解和自然语言查询 |

桌面管理端以独立应用窗口和应用图标呈现，不要求用户在浏览器中打开管理页面。桌面 UI 可以使用 Web 技术实现，但产品交付形态必须是独立 Windows 应用。

## 2. 背景与机会

### 2.1 当前工作流

目标用户通常在 gooood、小红书、Pinterest 等网站寻找参考图，并逐张执行：

“右键 → 图片另存为 → 选择目录 → 手动命名”

一次典型的概念设计找图任务需要保存约 30～100 张图片。

### 2.2 用户问题

1. **采集低效**：重复选择目录和命名，打断浏览与灵感判断。
2. **整理负担高**：图片下载后需要人工分类，容易堆积为无结构文件夹。
3. **来源易丢失**：本地文件与原网页脱离，后续难以核查项目背景。
4. **复用困难**：用户记得图片的感觉、材料或空间，却记不起文件名和目录。
5. **工具链断裂**：下载工具、文件夹、网页收藏与素材软件之间缺少统一闭环。

### 2.3 产品机会

现有图片下载能力主要优化“获得文件”，素材管理能力主要依赖人工标签。本产品的机会是把网页采集、受控 AI 标签、人工反馈和自然语言搜索连接起来，并保持私人原图本地存储。

## 3. 产品目标与非目标

### 3.1 MVP 目标

1. 将单张网页图片采集缩短为“选择项目类型后一次点击”，项目名称和文件夹由网页标题自动生成。
2. 让新图片自动完成 AI 解析，并将异常集中到可重试队列。
3. 用关键词标签体系建立稳定、可人工修正的图片描述。
4. 让自然语言成为顶部搜索主入口，同时保留左侧按项目浏览和按关键词筛选。
5. 在 5,000 张本地图片规模下保持可用性能。
6. 通过邀请测试验证用户是否在第 8～10 天重新使用历史素材。

### 3.2 Non-goals

MVP 明确不做：

- 整页图片扫描、整页勾选和自动批量下载
- macOS、Edge、Firefox、Safari 和移动端
- 用户云端图库和跨设备同步
- 团队空间、成员权限、评论和多人协作
- 公共素材资源库
- 以图搜图
- 视觉相似检测和自动归组
- GIF、SVG、AVIF、视频和动图
- AI 自动修改项目类型或物理文件夹
- 本地 AI 模型
- 正式订阅、点数充值和计费系统
- 项目、收藏夹或来源清单导出
- 本地备份和恢复
- 绕过登录、付费墙、防盗链、验证码或网站访问限制
- 材料节点和施工构造知识库
- 汇报排版或 moodboard 制作工具
- 浏览器页面形态的素材管理后台

## 4. 目标用户

### 4.1 核心用户画像

**方案设计师**

- 职业：建筑、室内或景观设计师
- 工作阶段：概念设计、方案前期、案例研究
- 行为：高频浏览设计网站和内容平台，每次保存 30～100 张参考图
- 当前工具：浏览器“另存为”与 Windows 项目文件夹
- 主要痛点：采集操作重复、命名随意、来源丢失、后期找不到
- 核心动机：快速建立与当前项目相关的灵感集合，并让素材未来仍可复用

### 4.2 次要用户画像

**长期素材积累者**

- 持续维护跨项目参考图库
- 更重视标签一致性、搜索准确性和历史复用
- 可能已有大量本地图片，需要导入和持续监听文件夹

### 4.3 暂不服务的用户

- 需要团队 DAM 或企业权限系统的设计机构
- 需要施工节点、BIM 构件或技术规范知识库的专业人员
- 需要批量抓取整站内容的下载用户
- 需要跨设备云图库的用户

## 5. JTBD 与核心场景

### 5.1 核心 JTBD

当我在项目前期浏览设计案例时，我希望不中断思路地保存真正需要的参考图，并让系统理解图片特征，以便在当前项目或未来项目中用自然语言快速找回。

### 5.2 次级 JTBD

1. 当我已有大量本地参考图时，我希望把它们导入同一素材库统一检索。
2. 当 AI 对图片理解不准确时，我希望快速修正单张或整批标签。
3. 当我只记得空间、风格或材料特征时，我希望用一句自然语言找到相关图片。
4. 当我重新使用图片时，我希望能打开原图、返回来源或拖入设计软件。

### 5.3 核心场景

#### 场景 A：为新项目采集灵感

用户在插件中选择“文化建筑”等默认项目类型并开启图片保存功能。鼠标经过目标图片时出现悬浮工具条；首次点击工具条中的“保存”时，系统以网页标题自动建立项目名称文件夹，后续图片继续保存到该文件夹。用户也可在工具条中切换类型，该选择同时成为后续图片的新默认值。

#### 场景 B：自动 AI 解析

图片保存后自动进入解析队列并生成描述与关键词标签；临时失败自动重试 2 次，仍失败后进入“解析失败”等待人工重试。

#### 场景 C：人工修正标签

用户在右侧详情面板修正描述或关键词标签，修改自动保存；也可以批量为多张图片增加“木结构”等关键词标签。

#### 场景 D：自然语言找回素材

用户输入“适合山地游客中心、带自然采光的木结构公共空间”，查看系统解析的查询条件，并调整标签获得结果。

#### 场景 E：复用历史图片

用户打开满意图片，将其复制到其他项目、加入收藏夹、复制或拖入设计软件，必要时返回来源网页。

## 6. 产品原则

1. **主动选择优先**：只有用户明确点击的网页图片才会采集。
2. **文件夹习惯优先**：磁盘目录使用“项目类型 / 网页标题项目名”两级结构。
3. **AI 可解释、可校正**：AI 结果必须显示来源和状态，不能成为黑箱。
4. **预设词库优先**：AI 优先从当前有效词库选择关键词；预设不足时可自主新增少量标签，用户创建和 AI 新增的词都会进入个人词库。
5. **搜索结果优先于搜索模式**：用户不需要判断关键词或自然语言模式。
6. **文件行为透明且可恢复**：修改项目归属同步移动真实文件；删除操作明确写为“移到回收站”。
7. **合法权限优先**：不绕过网站的访问控制和技术限制。

## 7. 产品范围与优先级

| 模块 | 能力 | 优先级 |
| --- | --- | --- |
| 邀请与账号 | 邀请码、注册、登录、AI 额度识别 | P0 |
| 桌面应用 | 独立窗口、本地核心模块、素材目录、后台任务、插件连接 | P0 |
| 网页保存 | 图片保存功能开关、项目类型选择/自定义、单图保存与离线暂存反馈 | P0 |
| 网站适配 | 通用网页、gooood、小红书、Pinterest | P0 |
| 本地导入 | 单图、多图导入 | P0 |
| 文件夹监听 | 绑定目录、发现变化、生成受管理副本 | P0 |
| 图库 | 瀑布流、图片卡片、详情、来源 | P0 |
| 组织 | 项目类型、网页项目、全局库、收藏夹 | P0 |
| 未解析 | 跨项目查看未解析、解析中和解析失败图片 | P0 |
| AI 分析 | 自动解析、进度、自动重试、单张/批量人工重试 | P0 |
| 关键词标签 | 预设、自定义、单张与批量编辑 | P0 |
| 标签词库 | 重命名、新增、归档、恢复 | P0 |
| 搜索 | 关键词、标签、自然语言混合搜索 | P0 |
| 查询解释 | 可编辑的查询理解条件 | P0 |
| 复用 | 打开、来源、定位、复制、拖拽 | P0 |
| 数据统计 | 关键事件、详细测试行为、删除与退出 | P0 |
| 目录迁移 | 更换产品素材目录并安全迁移 | P1 |
| 安装优化 | 一键安装、自动连接和诊断 | P1 |
| 云同步/协作 | 跨设备、团队空间 | P2 |

## 8. 信息架构

### 8.1 浏览器插件

- 图片保存功能开关
- 项目类型选择
- 始终可见的自定义项目类型输入框
- “设为默认”按钮
- 轻量桌面端连接状态
- 临时保存结果反馈
- 打开桌面素材管理应用

插件不承载网页项目名编辑、AI 额度、账号、图库、AI 分析、关键词、搜索和设置。

### 8.2 Windows 桌面素材管理应用

- 首页
  - 产品意图：“用一句设计意图，找回一张参考。”
  - 统一搜索框
  - 查询理解条件
  - 当前项目范围与关键词条件
  - 最近添加图库，首页默认展示 8 张
  - 次级未解析队列状态模块
- 全局素材库
- 未解析
- 按项目浏览
  - 项目类型
  - 项目名称
- 按关键词浏览
  - 关键词搜索
  - 空间类型
  - 建筑风格
  - 主要材料
  - 自定义关键词
- 收藏夹
  - 收藏夹列表
  - 收藏夹详情
- AI 任务
  - 待分析
  - 处理中
  - 已完成
  - 失败
- 关键词管理
  - 空间、材料、构造、形态、风格、氛围和其他
  - 自定义关键词
  - 已归档关键词
- 图片详情
- 导入与监听文件夹
- 设置
  - 素材目录
  - 账号与额度
  - 数据与隐私
  - 连接诊断
  - 版权说明

## 9. 详细功能需求

### 9.1 邀请、账号与首次启动

#### FR-001 邀请码注册｜P0

用户必须使用有效邀请码注册基础账号。

验收标准：

- 无效、过期或已耗尽的邀请码不能完成注册。
- 注册成功后账号可识别测试资格和 AI 额度。
- 账号不上传或同步桌面端本地素材库。
- 登录失败不会删除本地数据。

#### FR-002 首次启动｜P0

首次启动需要完成桌面应用初始化、素材库建立方式选择、根目录权限检查、内部本地核心模块启动、插件连接和隐私说明。

验收标准：

- 提供“创建新素材库”“连接已有素材库”“移动现有素材库”三种建立方式，默认推荐创建新库。
- 用户必须选择素材库根目录；通过读取、写入和创建目录权限检查后才能继续。
- 权限不足、非本地路径或路径不可用时明确说明原因，并保留用户当前选择。
- 确认前预览“素材根目录 / 项目类型 / 项目名称”的真实两级目录结构。
- 桌面应用内部自动启动文件、数据库、索引和监听模块，不要求用户单独安装第二个本地服务。
- 明确显示插件与桌面应用是否连接。
- 连接失败时给出可执行的诊断提示。
- 用户在首次采集前确认版权使用说明。
- 详细行为统计需单独说明并取得明确同意；拒绝不影响核心功能。
- 桌面窗口关闭且仍有 AI 或目录监听任务时，明确询问继续后台运行或完全退出。

#### FR-003 桌面运行状态｜P0

桌面应用支持前台窗口、后台运行和完全退出三种状态。

验收标准：

- 桌面窗口打开或后台运行时，插件可以连接并采集。
- 最小化或关闭窗口选择“继续后台运行”后，文件夹监听和 AI 任务继续。
- 完全退出后停止目录监听，安全暂停或取消未完成任务，并在插件中显示“桌面应用未运行”。
- 插件可唤起已经安装的桌面应用。
- 应用更新不得造成图片、索引或人工关键词丢失。

### 9.2 项目类型、自动项目与收藏夹

#### FR-010 管理项目类型和收藏夹｜P0

插件内置办公建筑、学校建筑、文化建筑等项目类型。用户可以新增、重命名和归档个人项目类型，并可以创建、重命名和删除收藏夹。

验收标准：

- 项目类型和收藏夹名称去除首尾空白后不能为空；纯空格、纯换行或制表符均不能通过校验。
- 项目类型用于第一层物理文件夹。
- 用户新增的项目类型同步进入个人项目类型词库。
- 已包含项目文件夹的项目类型不能直接永久删除，只能归档或迁移。
- 删除收藏夹只删除逻辑关联，不删除磁盘图片。

#### FR-011 设置当前项目类型｜P0

用户在采集前只需要在插件中选择当前项目类型，不需要手动输入项目名称。

验收标准：

- 未选择项目类型时不能开启图片保存功能或使用悬浮工具条保存。
- 当前项目类型在插件中持续可见。
- 当前项目类型保持到用户主动切换。
- 用户可在插件内通过第一层级、始终可见的输入框新增并选择自定义项目类型。
- 自定义项目类型提交前必须去除首尾空白；校验失败时不得创建类型或保存当前图片，并在输入框附近显示错误。
- 当前项目类型是物理存储依据，AI 分析不得自动切换类型或移动目录。

#### FR-012 自动生成项目名称｜P0

首次采集当前网页图片时，系统以网页标题自动生成第二层项目名称文件夹。

验收标准：

- 用户无需在采集前填写项目名称。
- 项目名称默认来自当前网页标题。
- 用户可在桌面应用中修改自动生成的项目名称。
- 同一网页后续图片复用同一项目文件夹。
- 无有效网页标题时使用“网站名称＋采集日期”。
- 自动清理 Windows 文件名非法字符并处理过长标题。
- 同名标题但来源 URL 不同时，使用网站名或编号避免目录冲突。
- 修改项目名称时同步重命名二级文件夹。
- 修改项目类型时移动整个二级项目文件夹。

### 9.3 网页图片采集

#### FR-020 图片保存功能｜P0

用户主动开启图片保存功能后，鼠标经过可保存的内容图片会显示极简悬浮工具条；用户确认或调整项目类型并点击“保存”后保存当前单张图片。

验收标准：

- 浏览器右上角弹窗只包含图片保存功能开关、预设类型下拉框、始终可见的自定义输入框和“设为默认”。
- 弹窗底部保留“打开桌面应用”和“打开素材文件夹”两个轻量辅助操作；后者直接打开包含全部一级项目类型目录的素材库根目录。
- 连接状态和保存结果以轻量状态点或临时消息呈现，不形成独立功能区。
- 自定义输入不作为下拉框选项，始终直接显示在第一层级。
- 预设选择和自定义输入互斥：输入自定义内容时取消预设选择，选择预设或“我的类型”时清空自定义输入。
- 未选择有效项目类型时不能开启图片保存功能。
- 插件记住最近一次有效项目类型，并在右上角弹窗和网页悬浮工具条中共享。
- 悬浮工具条始终包含预设类型下拉框、自定义类型输入框和“Save”三个第一层级控件。
- 悬浮工具条中的项目类型默认继承全局默认值；用户点击“Save”后，所选类型才同时用于当前图片并更新后续默认值。
- 弹窗点击“设为默认”只更新后续默认类型，不触发图片保存。
- 有效自定义类型提交后进入“我的类型”分组，后续可以从下拉框直接选择。
- 仅在图片保存功能开启且鼠标经过可保存的内容图片时显示工具条；图标、头像和按钮素材等非内容图片不显示。
- 图片保存功能关闭时不拦截网页正常点击。
- 图片保存功能开启时网页图片本身仍保持原有点击行为，只有工具条中的“保存”触发保存。
- 页面存在灯箱或模态画廊时，只允许最上层可见画廊内的当前内容图触发工具条，遮罩后的图库图片不得参与命中。
- 用户点击 Save 后锁定当前图片目标，直至保存成功或失败；背景指针事件不得替换目标。
- 每次点击“保存”只处理一张用户指定图片。
- 首次保存时自动建立“项目类型 / 网页标题项目名”目录。
- 保存结果直接写入当前网页对应的二级项目文件夹。
- 不自动扫描、列出或下载整页图片。
- 保存成功后显示“项目类型 / 项目名称”的最终归档位置，并提供“打开所在文件夹”；该入口仅在成功或重复反馈中临时出现，不中断连续浏览。
- 正式归档确认后清除 Chrome 中对应的临时下载历史项，避免最终文件已经移动时仍显示“已删除”。
- 工具条至少覆盖就绪、自定义输入、保存中、保存成功、已离线暂存、当前项目内重复、失败和格式不支持状态。
- 保存状态以 `captureId` 持久化短期回执；即使图片下载与归档早于网页完成请求响应，工具条也必须补取最终状态，不能永久停留在“保存中”。
- 工具条放置在图片内部上方居中并距顶部约 8 像素；宽图三个控件单行排列，窄图允许自定义输入换到第二行；离开图片后收起，但保存中与结果反馈完成前不得丢失状态。
- 用户可随时关闭图片保存功能。
- 插件中不显示或编辑网页项目名称，不显示 AI 额度、历史记录、账号、图库、AI、关键词、搜索和设置。
- “打开素材文件夹”只提供系统文件夹快捷入口，不在插件内展开目录树或承担文件管理。

#### FR-021 最佳可用图片获取｜P0

系统优先保存用户有权访问的最高分辨率版本。

验收标准：

- 支持 JPEG、PNG、WebP。
- 优先识别当前图片对应的高分辨率资源。
- 原始版本不可访问时回退到最佳可用版本。
- 不绕过登录、付费墙、防盗链、验证码和访问控制。
- 失败时显示原因或可理解的失败分类。

#### FR-022 来源记录｜P0

采集图片时同步记录基础来源。

必需字段：

- 原始图片 URL
- 来源页面 URL
- 来源网页标题
- 网站名称
- 采集时间

验收标准：

- 图片详情中可以查看来源字段。
- 来源页面可从产品中打开。
- 来源字段缺失时不阻止保存，但必须标明缺失。

#### FR-023 完全重复去重｜P0

系统通过文件内容判断完全重复图片。

验收标准：

- 同一项目文件夹内，相同文件即使名称或 URL 不同也只保留一份。
- 同一项目内重复采集时提示“已存在”。
- 同一图片属于不同项目时，允许在各项目文件夹保存独立副本。
- 全局索引可以识别跨项目副本内容相同，但不得阻止复制。
- 不把不同尺寸、裁切或压缩版本判定为视觉重复。

#### FR-024 网站兼容｜P0

提供通用网页基础能力，并重点适配 gooood、小红书、Pinterest。

验收标准：

- 三个重点网站分别记录成功率。
- 小红书支持无文件后缀、但由 URL 参数或路径标识实际 JPEG/PNG/WebP 格式的内容图片。
- 对需要来源页访问条件的图片，优先在当前页面上下文取得图片数据，再进入统一暂存与归档链路；不得绕过登录或网站访问限制。
- Pinterest 与 ArchDaily 的图片即使存在点击覆盖层，也能通过鼠标位置识别下层内容图。
- ArchDaily 懒加载图片优先读取真实资源属性，不把 1×1 占位图当作保存目标。
- 网站更新导致适配失败时可识别为站点兼容错误。
- 仅处理用户当前正常可见和有权访问的内容。

#### FR-025 桌面端退出后的离线暂存｜P0

桌面端关闭窗口时继续在系统后台运行；用户选择完全退出后，插件仍允许下载并进入离线暂存模式。

验收标准：

- 图片下载到浏览器默认下载目录下的固定待导入目录，并记录项目类型、项目名称、来源与保存时间。
- 成功反馈明确显示“已暂存，打开桌面端后自动整理”，不显示为已完成正式归档或 AI 解析。
- 下次启动桌面端后自动导入、移动到正式两级目录、去重并进入解析队列。
- 自动导入确认后同步清除对应临时下载历史项，并允许用户从结果反馈定位最终文件。
- 暂存下载失败时当场显示真实失败原因和重试入口。

### 9.4 本地图片导入与文件管理

#### FR-030 文件导入｜P0

用户可以选择单张或多张 JPEG、PNG、WebP 导入素材库。

验收标准：

- 导入文件被复制到产品管理目录。
- 导入后进入全局素材库。
- 用户可选择目标项目类型和项目名称；加入收藏夹只建立逻辑关联。
- 导入过程应用完全重复去重。
- 原始文件后续删除不影响产品管理副本。

#### FR-031 监听文件夹｜P0

用户可以绑定本地文件夹，持续发现图片变化。

验收标准：

- 首次绑定时扫描支持格式图片。
- 新增文件可被发现并导入为产品管理副本。
- 源目录删除文件时，不删除产品管理副本。
- 用户从素材库移除图片后记录忽略状态，避免再次导入。
- 监听错误不影响已有素材。

#### FR-032 产品素材目录｜P0

产品按“素材根目录 / 项目类型 / 项目名称 / 图片”建立两级项目目录。

验收标准：

- 首次启动要求用户选择素材库根目录，并通过读取、写入和建目录权限检查。
- 设置页展示当前目录和占用空间。
- 第一层目录来自当前项目类型。
- 第二层目录默认来自网页标题并允许用户修改。
- 不同项目之间允许存在相同内容的独立文件副本。
- 收藏夹和“未解析”只建立逻辑索引，不创建额外图片副本。
- 产品不得在用户未确认的情况下删除目录内原图。

#### FR-033 素材目录迁移与切换｜P1

设置中分别提供“移动现有素材库”和“连接另一个素材库”。

验收标准：

- 迁移前检查目标空间和写入权限。
- 迁移中断后能继续或回滚到可用状态。
- 迁移完成后更新全部路径和索引。
- 不产生无提示的数据丢失。
- 单个素材库不能默认跨越多个根目录。

### 9.5 图库与图片详情

#### FR-040 首页图库｜P0

首页顶部先表达产品意图“用一句设计意图，找回一张参考。”，下方紧随自动聚焦的统一搜索框；主体仅保留最近添加图片，默认以两排完整网格展示 8 张。页面底部提供次级未解析队列模块，状态清晰但不与图库争夺主视觉。

验收标准：

- 输入时显示历史搜索和关键词建议，按 Enter 后执行自然语言搜索。
- 图片卡片显示缩略图、解析状态、描述摘要、已有关键词标签和主要匹配原因。
- 首页不展示最近项目；未解析队列应展示等待、解析中、需重试和进度，但不得使用高强调侧边色块或大面积进度条抢占注意力。
- AI 与人工关键词标签使用两套克制但可辨识的色系区分，不在每个标签后重复显示来源文字；无障碍名称仍保留来源信息。
- 支持按项目类型、项目名称、收藏夹和关键词过滤。
- 左侧栏同时提供“按项目浏览”和“按关键词浏览”，两者均为一级核心能力。
- 一级导航显示“未解析”入口及数量。
- 5,000 张素材规模下首屏 3 秒内可用。

#### FR-041 图片详情｜P0

图片详情从右侧面板打开，并保留底层瀑布流位置。面板展示：

- 大图
- 文件基础信息
- 来源信息
- 所属物理项目和逻辑收藏夹
- AI 或人工来源的关键词标签
- 图片描述
- AI 分析状态

可执行操作：

- 点击头图上的“查看原图”进入覆盖应用窗口的原图查看器；关闭后返回当前详情位置
- 编辑描述，以及新增、删除或修改关键词标签；“添加关键词”使用一个统一输入框：输入时筛选素材库已有词，点击候选复用，输入新词后提交则创建；仅标签右侧“×”执行删除，点击标签正文不删除；修改自动保存，并支持连续撤销最近 50 次单图编辑
- 修改项目类型或项目名称，并同步移动真实文件
- 加入/移出收藏夹
- 重新执行 AI 分析
- 打开来源
- 在文件夹中定位
- 复制或拖拽图片
- 移到系统回收站

#### FR-042 移到回收站｜P0

验收标准：

- 操作名称明确显示为“移到回收站”。
- 本地文件进入系统回收站，同时移除图库索引和关联关系。
- 完成后提供短时间撤销。
- 失败时保留原文件和素材库记录。

#### FR-043 未解析视图｜P0

“未解析”是跨项目逻辑视图，显示所有未完成 AI 解析的图片。

状态定义：

- 未解析：从未生成完整描述和关键词。
- 解析中：已有 AI 任务正在处理。
- 解析失败：请求失败或结果缺少描述/关键词。
- 已解析：同时具有图片描述和至少一个关键词，不再出现在未解析视图。

验收标准：

- 新保存和新导入图片自动进入“未解析”并自动排队解析。
- 一级导航持续显示所有未完成任务数量，即“未解析 + 解析中 + 解析失败”。
- 页面默认混合展示全部待处理任务，并提供等待中、解析中、解析失败状态筛选；顶部显示各状态数量和整体进度。
- 导航角标、首页摘要、页面统计与筛选数量必须由同一状态数据实时计算。
- 临时失败自动重试 2 次；仍失败后显示原因与单张、批量重试入口。
- 未解析视图不移动或复制磁盘文件。

#### FR-044 双路径侧栏浏览｜P0

左侧栏同时提供两种素材浏览路径：

1. 按项目浏览：按照物理目录“项目类型 / 项目名称”展开并查看图片。
2. 按关键词标签浏览：按照空间、材料、构造、形态、风格、氛围和其他分组查看图片。

验收标准：

- 项目类型可展开项目名称，并显示对应图片数量。
- 关键词按维度分组，支持在侧栏内搜索，并显示关联图片数量。
- 预设与自定义关键词标签均可拖动到其他分类；操作只更新个人侧栏分类，不改变标签值、图片关联、搜索结果或系统原始语义维度。
- 分类拖动完成后提供短时间撤销；分类区域是放置目标，标签之间不通过拖拽执行合并。
- 未解析图片没有关键词时，仍可通过全局、未解析和项目路径找到。
- 项目范围和关键词可以单独使用，也可以组合筛选。
- 当前项目范围和关键词以可清除条件显示在内容区顶部。
- 清除项目范围不会清除关键词，清除关键词不会清除项目范围。
- 点击同一关键词可取消该筛选；项目范围与多个关键词之间采用 AND。
- 提交新的非空自然语言查询时，自动清除提交前遗留的项目与关键词条件，并提示正在检索整个素材库；结果出现后用户再次主动选择项目或关键词时，才作为二次精确筛选与当前查询采用 AND。

### 9.6 AI 分析

#### FR-050 自动解析与人工重试｜P0

新图片自动进入解析队列；人工操作用于失败重试或主动重新解析。支持以下范围：

- 单张图片
- 用户自由多选
- 未解析视图中的选中或全部图片
- 当前项目全部图片
- 当前收藏夹全部图片

验收标准：

- 新保存或新导入图片自动运行 AI 解析。
- 临时失败自动重试 2 次，超过上限后进入解析失败。
- 手动重新解析前显示影响图片数量。
- 已分析图片可由用户主动重新分析。

#### FR-051 AI 任务状态｜P0

任务状态包括：

- 等待中
- 排队中
- 处理中
- 成功
- 部分成功
- 失败

验收标准：

- 用户能看到队列总量、完成量和失败量。
- 单张失败不阻止整批任务继续。
- 失败图片可以单独重试。
- 断网或服务异常不会丢失本地图片和人工标签。

#### FR-052 AI 输出｜P0

每张图片的 AI 输出包括：

- 图片语义描述
- 从预设词库选择或新生成的关键词标签
- 图文检索向量
- 模型版本和分析时间

验收标准：

- 只有同时生成描述和至少一个关键词才标记为已解析。
- AI 关键词标签可立即参与搜索和分类筛选，并标明 AI 来源。
- 用户可以删除、修改或新增自定义关键词标签；人工修改自动保存并优先于 AI 结果。
- AI 建议的项目类型关键词与当前物理项目类型不一致时，只显示差异提示，不自动移动文件夹。
- 用户已拒绝的同一图片—关键词组合不得在普通重试中反复出现。

### 9.7 标签与词库

#### FR-060 基础词库｜P0

上线前由 AI 生成、产品方人工审核基础词库，包含空间、材料、构造、形态、风格和氛围等维度；无法归类的自定义词进入“其他”。

基础词库不限定固定数量，但发布前必须完成去重、同义词合并和歧义审核。

“项目类型”词库同时用于插件第一层目录选择和图片分类筛选。

#### FR-061 个人词库编辑｜P0

用户可以：

- 重命名个人预设标签
- 新增标签
- 归档标签
- 恢复归档标签

验收标准：

- 修改只影响当前用户。
- 重命名同步更新所有关联图片。
- 已被使用的标签不能直接永久删除，只能归档。
- 归档标签不再参与新图片 AI 推荐。
- 历史图片继续展示归档标签。

#### FR-062 自定义标签｜P0

自定义标签配置包括：

- 名称：必填
- 简短定义：选填
- 参考图片：选填

验收标准：

- 创建后立即进入后续 AI 分析的候选词库。
- 标签名称冲突时提示合并或修改。
- AI 识别效果不佳时可提示用户补充定义或参考图。
- AI 优先使用预设标签；预设不足时每张图片最多自主新增 5 个标签，新增词直接关联当前图片、进入“其他”分类，并进入后续 AI 候选词库。
- AI 新增标签写入前必须执行名称归一化和完全重复检查，并在提示中要求避免近义词；用户可随时删除、改名、合并或移动分类。
- 用户也可创建自定义标签，确认后立即用于当前图片并进入后续 AI 候选词库。

#### FR-063 关键词标签校正｜P0

支持：

- 单张增加、删除、修改
- 多选图片批量增加或删除
- 查看标签来源和状态

验收标准：

- 批量操作前显示影响图片数量。
- 人工修改不会在普通重新分析时被覆盖。
- 编辑自动保存，显示完成状态；每张图片独立保留最近 50 次编辑历史，支持连续撤销。

### 9.8 混合搜索

#### FR-070 统一搜索框｜P0

同一入口接受短关键词和完整自然语言，不要求用户切换搜索模式。

搜索框固定在桌面应用内容区顶部：

- 没有侧栏条件时，默认搜索整个素材库。
- 提交新的自然语言查询时，以该查询作为新的全库检索基准，自动清除之前遗留的项目范围和关键词条件。
- 应用启动后搜索框自动聚焦；输入时显示历史搜索和关键词建议，按 Enter 后执行自然语言搜索。
- 输入过程中不逐字请求 AI 搜索。
- 清除旧条件时显示轻量反馈；搜索结果区说明当前是全库自然语言检索，还是用户在结果后主动增加了精确筛选。
- 用户可以分别清除自然语言、项目或关键词条件。

检索信号包括：

- 文件名
- AI 图片描述
- 系统预设关键词
- 用户自定义关键词
- 图文语义向量

#### FR-071 查询理解｜P0

系统将自然语言解析成可编辑条件。

验收标准：

- 显示系统识别出的标签、文本概念和范围条件。
- 用户可删除或修改解析条件。
- 修改条件后结果立即重新计算。
- 解析失败时仍执行关键词和语义检索，并给出提示。

#### FR-072 搜索排序与解释｜P0

验收标准：

- 综合项目类型、项目名称、关键词、图片描述和语义相似度排序。
- 权重可以在测试阶段调整，不写死在客户端。
- 结果应展示主要匹配原因。
- AI 与人工关键词标签均可参与检索；用户修改内容优先于 AI 结果。
- 搜索结果采用图片瀑布流，并显示命中的关键词标签或描述片段。
- 在线自然语言搜索 5 秒内返回。
- 本地关键词标签搜索 2 秒内返回。

#### FR-073 离线搜索｜P0

断网时：

- 左侧项目浏览和关键词筛选可用。
- 关键词搜索可用。
- 已有图库和人工编辑可用。
- 自然语言混合搜索不可用，并明确说明原因。

### 9.9 素材复用

#### FR-080 图片使用操作｜P0

用户可以：

- 打开大图
- 打开来源网页
- 在 Windows 文件夹中定位
- 复制图片
- 拖拽图片到其他设计软件

验收标准：

- 原文件缺失时显示状态并提供定位帮助。
- 打开来源使用记录的来源页面 URL。
- 复制或拖拽失败时不修改原图。

### 9.10 数据统计与隐私

#### FR-090 测试数据采集｜P0

测试阶段需要收集：

- 采集与导入
- AI 分析与失败
- 标签确认、拒绝和人工修改
- 搜索词与查询改写
- 搜索结果浏览
- 复制到其他项目、打开来源、复制和拖拽等复用行为
- 客户端与站点兼容错误

#### FR-091 用户授权｜P0

验收标准：

- 注册阶段单独展示详细统计说明。
- 只有用户明确同意后才收集搜索原文和详细路径。
- 拒绝或撤回不影响采集、AI、管理和搜索核心功能。
- 用户可在设置中关闭统计。
- 用户可申请删除尚在保存期内的详细数据。

#### FR-092 数据保存｜P0

验收标准：

- 详细行为数据保存 30 天。
- 到期后删除或完成不可逆匿名化。
- 图片本身不得进入行为统计系统。
- AI 临时图片处理完成后删除。
- 用户图片不得默认用于模型训练或公共素材库。

## 10. 关键业务规则

### 10.1 图片唯一性

- 每个受管理图片拥有稳定内部 ID。
- 完全重复以文件内容摘要为判断依据。
- 完全重复去重仅阻止同一项目文件夹内的重复保存。
- 同一内容进入不同项目时建立独立文件副本和独立 Asset 记录。
- 收藏夹只建立逻辑关联，不生成图片副本。
- 不同压缩、尺寸或裁切版本视为不同文件。

### 10.2 关键词标签来源与编辑

| 当前来源 | 用户操作 | 结果 |
| --- | --- | --- |
| AI 生成 | 修改 | 保存为用户修改，优先于 AI 结果 |
| AI 生成 | 删除 | 从图片移除并记录人工删除 |
| 用户创建 | 修改 | 更新图片与搜索索引 |
| 用户创建 | 删除 | 从图片移除，不删除词库标签 |
| 用户修改 | 重新分析 | 保持人工内容，不被普通重新分析覆盖 |

### 10.3 标签归档

- 已使用标签只能归档。
- 归档不删除历史关联。
- 归档标签不参与后续 AI 候选。
- 恢复后重新进入候选词库。

### 10.4 AI 额度

- 测试期免费但限额。
- 额度按账号识别。
- 执行分析前显示预计消耗。
- 失败且未产生有效结果的请求是否扣额，由服务端计费策略决定，但必须向用户展示最终扣减结果。

### 10.5 删除

- 从产品中移除项目或收藏夹不自动删除磁盘项目文件夹或图片。
- 从素材库移除图片不删除磁盘文件。
- 产品不提供永久删除本地原文件的入口。

### 10.6 版权

- 用户必须主动选择图片。
- 产品保存来源但不判断或授予版权。
- 产品不得绕过访问控制。
- 版权提示不能代替用户取得合法授权。

## 11. 状态与异常设计

### 11.1 插件状态

- 未安装或未启动桌面应用
- 未登录
- 未选择当前项目类型
- 当前网页项目名待生成
- 就绪
- 图片保存功能开启
- 正在保存
- 保存成功
- 文件重复
- 格式不支持
- 原图不可访问
- 网站限制
- 本地写入失败

### 11.2 本地文件状态

- 正常
- 原文件缺失
- 索引待更新
- 重复
- 已移到回收站
- 来自监听目录且已忽略

### 11.3 AI 状态

- 未解析
- 等待中
- 排队中
- 解析中
- 已解析
- 解析失败
- 结果不完整
- 网络失败
- 模型失败
- 额度不足
- 已取消

### 11.4 搜索降级

- 自然语言服务不可用时，保留关键词和标签搜索。
- 查询解析失败时，不清空用户输入。
- 向量索引缺失时，返回关键词和标签结果并显示降级提示。

## 12. 概念数据模型

| 实体 | 关键字段 |
| --- | --- |
| User | user_id、invite_status、AI_quota、analytics_consent |
| Asset | asset_id、project_id、managed_path、content_hash、format、dimensions、parse_status、created_at |
| Source | asset_id、image_url、page_url、page_title、site_name、captured_at |
| ProjectType | project_type_id、name、origin、archive_status、folder_path |
| Project | project_id、project_type_id、name、source_url、folder_path、created_at、status |
| Collection | collection_id、name、created_at |
| AssetCollection | asset_id、collection_id |
| Tag | tag_id、dimension、name、definition、origin、archive_status |
| AssetTag | asset_id、tag_id、state、confidence、source_model、updated_at |
| AIAnalysis | analysis_id、asset_id、model_version、status、description、embedding_version |
| WatchedFolder | folder_id、path、status、last_scan_at |
| IgnoreRecord | source_path、content_hash、ignored_at |
| SearchSession | session_id、query、parsed_conditions、result_count、created_at |

要求：

- 所有核心实体使用稳定 ID，为未来同步预留。
- ProjectType 与 Project 必须可追踪到实际第一、第二层文件夹路径。
- 模型版本、词库版本和向量版本必须可追踪。
- 本地图片、项目和标签数据不以云端账号数据为唯一来源。

## 13. 数据埋点方案

### 13.1 核心事件

| 事件 | 触发时机 | 核心属性 |
| --- | --- | --- |
| onboarding_completed | 完成首次启动 | 浏览器、连接结果、统计授权 |
| project_type_selected | 设置当前项目类型 | 预设或自定义、类型 ID |
| web_project_created | 首次采集时生成网页项目 | 标题来源、是否改名、冲突处理 |
| extension_enabled | 开启图片保存功能 | 网站 |
| asset_capture_succeeded | 单图保存成功 | 网站、格式、耗时、是否回退 |
| asset_capture_failed | 单图保存失败 | 网站、失败类型 |
| duplicate_detected | 检测到重复 | 来源类型 |
| local_import_completed | 完成本地导入 | 数量、重复数、失败数 |
| ai_analysis_started | 提交分析 | 范围、图片数、预计额度 |
| ai_analysis_completed | 完成分析 | 成功数、失败数、耗时、模型版本 |
| parse_backlog_viewed | 打开未解析视图 | 未解析、处理中、失败数量 |
| keyword_manually_edited | 修改关键词标签 | 维度、来源、操作类型 |
| ai_keyword_created | AI 自主新增词库外关键词 | 模型版本、标签名称、图片 ID |
| custom_keyword_created | 用户创建自定义关键词 | 维度、是否包含定义或参考图 |
| project_scope_selected | 从左栏选择项目范围 | 项目类型、项目 ID、结果数 |
| keyword_filter_selected | 从左栏选择关键词 | 关键词 ID、维度、结果数 |
| search_submitted | 提交搜索 | 查询长度、解析条件数 |
| search_results_returned | 返回结果 | 耗时、结果数、是否降级 |
| search_result_opened | 打开结果 | 排名、匹配原因 |
| asset_reused | 发生复用 | 打开来源、复制到项目、复制、拖拽 |
| analytics_consent_changed | 修改统计授权 | 开启或关闭 |

### 13.2 指标口径

#### 激活用户

首次使用 3 天内同时完成：

- 采集或导入至少 10 张图片
- AI 分析至少 5 张图片
- 至少完成一次搜索

#### 有效复用

用户通过搜索或标签找到图片后，发生以下任一动作：

- 打开大图
- 返回来源
- 复制到其他项目
- 复制图片
- 拖拽图片

只打开产品或继续采集不计为有效复用。

#### AI 关键词标签保留率

解析后未被用户删除或修改的 AI 关键词标签数 ÷ 用户查看过的 AI 关键词标签总数。

系统预设标签和用户自定义标签分别统计。

#### 解析完整率

同时具有非空图片描述和至少一个关键词的已解析图片数 ÷ 标记为已解析的图片总数。

#### 采集成功率

成功保存的有效目标图片数 ÷ 用户点击且符合支持格式与访问条件的目标图片数。

## 14. Success Criteria

### 14.1 北极星指标

在不少于 30 名已激活测试用户中：

**10 日有效复用率 ≥40%**

统计窗口为用户首次使用后的第 8～10 天。

### 14.2 搜索质量

- 20 名用户，每人完成 5 个真实找图任务。
- 测试任务分别覆盖按项目浏览、按关键词浏览和顶部自然语言搜索。
- 至少一个任务要求组合项目范围、关键词与自然语言查询。
- 两次查询以内完成。
- 满意图片位于前 10 个结果。
- 完成时间不超过 60 秒。
- 任务成功率 ≥80%。

线上点击、停留和查询改写只作为诊断信号，不单独判断搜索成功。

### 14.3 AI 标签质量

- AI 关键词标签保留率 ≥80%。
- 至少 80% 的图片覆盖六个主要关键词标签维度中的三个。
- 标记为“已解析”的图片必须 100% 同时包含非空描述和至少一个关键词。
- 失败或结果不完整的图片必须继续出现在“未解析”视图。

### 14.4 采集效率

- 选择当前项目类型后，项目名称自动生成，单图采集只需一次点击。
- 采集 30 张目标图片的总耗时比浏览器原生“另存为”流程降低 ≥70%。

### 14.5 采集可靠性

- gooood、小红书和 Pinterest 分别统计。
- 有效目标图片采集成功率 ≥90%。

登录失效、内容删除和网站明确阻止访问的情况不计入有效样本。

### 14.6 性能

在约 5,000 张图片的普通 Windows 测试环境和稳定网络下：

- 图库首屏 3 秒内可用。
- 关键词标签搜索 2 秒内返回。
- 自然语言混合搜索 5 秒内返回。

## 15. MVP 发布计划

### 阶段 0：可行性验证

目标：

- 验证 Chrome 插件与 Windows 桌面应用内部核心模块通信。
- 验证三个重点网站的单图选择与最高可用分辨率获取。
- 验证云端图片理解、受控标签和语义检索链路。
- 验证 5,000 张本地图片的索引性能。

退出条件：

- 不存在阻断核心闭环的技术问题。
- 已明确 AI 服务的数据留存政策。

### 阶段 1：内部 Alpha

范围：

- 产品团队和少量内部用户。
- 完整跑通采集、分析、校正、搜索和复用。
- 修复数据丢失、重复、索引和安装问题。

退出条件：

- P0 主流程全部可用。
- 不存在导致本地图片或标签丢失的高优先级缺陷。

### 阶段 2：邀请 Beta

范围：

- 至少 30 名建筑、室内或景观设计师。
- 测试周期至少 10 天。
- 记录北极星指标、AI 标签质量、搜索任务和采集成功率。

退出条件：

- 完成 Success Criteria 评估。
- 得出继续、调整定位或停止投入的产品结论。

## 16. 发布门槛

以下条件全部满足后才能邀请真实用户：

1. P0 功能通过验收。
2. 本地图片和人工标签不存在已知数据丢失风险。
3. 插件在图片保存功能关闭时不干扰网页操作。
4. AI 图片处理后的删除或零留存机制已确认。
5. 用户隐私说明、统计授权、关闭和删除流程可用。
6. 版权提示和来源记录可用。
7. gooood、小红书、Pinterest 已完成基础兼容测试。
8. AI 额度和成本上限已配置。
9. 已准备用户测试任务、访谈提纲和问题反馈渠道。

## 17. 风险与应对

| 风险 | 影响 | MVP 应对 |
| --- | --- | --- |
| 网站 DOM 或资源策略频繁变化 | 采集失败率上升 | 通用策略与站点适配分离，记录站点错误 |
| 网页标题过长、为空或同名 | 文件夹创建冲突 | 清理非法字符，提供备用名称并按来源消歧 |
| 修改项目类型或名称需要移动目录 | 路径失效或迁移中断 | 迁移前校验、原子更新索引、失败可恢复 |
| 跨项目独立副本增加磁盘占用 | 5,000 张图库更快占满磁盘 | 显示占用空间和跨项目重复提示，但不阻止复制 |
| 登录态和防盗链限制 | 无法获取最高分辨率版本 | 不绕过限制，回退最佳可用版本并提示 |
| 悬浮工具条与网页原交互冲突 | 误操作或打断浏览 | 仅在图片保存功能开启时显示工具条，且只由“保存”按钮触发保存，不拦截图片原有点击 |
| AI 标签词库过大或歧义 | 标签确认率下降 | 上线前人工审核、同义词合并、分维度评估 |
| 自定义标签定义不足 | AI 误判 | 支持定义和参考图，提供补充提示 |
| 自然语言搜索成为黑箱 | 用户不信任结果 | 展示可编辑查询理解和主要匹配原因 |
| 桌面应用安装或插件配对复杂 | 用户在激活前流失 | 一体化安装、自动发现、状态检查和诊断 |
| 无备份导致数据损失 | 用户信任受损 | 明确提示风险，不自动删除原图 |
| AI 成本失控 | 测试无法持续 | 邀请制、账号额度、执行前显示预计消耗 |
| 图片和搜索词产生隐私风险 | 无法合法开展测试 | 单独授权、30 天删除、图片不进入分析埋点 |
| 跨境模型服务不确定 | 合规和可用性风险 | 模型层可替换，真实测试前完成评估 |

## 18. 外部依赖

- Chrome 插件能力与权限
- Windows 本地文件系统权限
- 桌面应用与浏览器插件的安全通信机制
- Windows 桌面应用打包、后台运行和升级机制
- 云端图片理解模型
- 文本与图像向量模型
- 邀请、账号和额度服务
- gooood、小红书、Pinterest 页面结构与访问策略
- 隐私政策、用户协议和版权提示文本

## 19. 待技术验证项

以下项目不改变产品范围，但必须在技术方案阶段确定：

1. 浏览器插件与桌面应用内部核心模块的通信方式。
2. Windows 桌面应用技术容器、安装包、后台运行和升级机制。
3. 本地数据库、缩略图缓存与向量索引方案。
4. 最高可用分辨率图片的通用识别策略。
5. 三个重点网站的适配方式和维护成本。
6. 云端 AI 服务商及其图片留存政策。
7. 混合搜索的初始排序权重。
8. 测试期每位用户的具体 AI 免费额度。
9. “普通 Windows 电脑”的最低硬件基线。
10. 基础标签词库的最终内容、版本和审核流程。

## 20. 产品评审清单

产品评审需确认：

- 核心用户是否足够聚焦
- 单图主动采集是否仍能显著提升效率
- Windows 桌面应用与插件的联合安装成本是否可接受
- 四维标签是否足以支撑第一版搜索
- 自定义标签立即进入 AI 候选是否可控
- 搜索质量测试能否反映真实复用
- 无备份和不删除原文件的边界是否表达清楚
- 行为数据授权方案是否适合真实测试
- MVP 范围是否能在预期资源内完成

## 21. 下一阶段交付物

PRD 评审通过后，依次产出：

1. 用户流程图与页面状态图（已完成）
2. 页面信息架构与低保真线框图（已完成）
3. 可点击原型和可用性测试方案（已完成核心页面）
4. [技术架构与数据模型设计](./MVP_TECH_ARCHITECTURE.md)（已完成 Draft v1.0）
5. API 与 AI 输入输出协议（已在技术架构文档中完成 MVP 级定义）
6. 开发 Epic、用户故事和测试用例
7. MVP 排期与风险验证计划
