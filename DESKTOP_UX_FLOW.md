# 桌面端用户流程与页面状态

> 状态：已确认，可进入低保真交互原型  
> 更新：2026-08-09  
> 适用范围：Windows 桌面素材管理应用，以及插件向桌面端交付图片的衔接流程

## 1. 核心体验原则

1. 自然语言搜索是首要入口，项目与关键词标签是精确筛选入口。
2. 新图片自动解析，用户不需要逐张提交或确认。
3. AI 结果立即可搜索；人工修正是可选行为，并始终覆盖对应 AI 内容。
4. 应用内项目结构与本地两级文件夹保持一致。
5. 默认操作不打断浏览；异常、冲突和不可恢复操作才要求用户处理。

## 2. 信息架构

### 2.1 全局框架

- 顶部：自然语言搜索框，应用启动后自动聚焦。
- 左侧一级入口：首页、全部素材、未解析、设置。
- 左侧“按项目浏览”：项目类型 / 项目名称两级树。
- 左侧“按关键词标签浏览”：提供新增标签、标签搜索，以及空间、材料、构造、形态、风格、氛围、其他分组；新增时必须选择所属分类，并校验空白与重名。
- 主内容区：共享图片瀑布流、搜索结果或状态页面。
- 右侧：图片详情面板，打开时不丢失瀑布流位置。

### 2.2 首页

按以下优先级排列：

1. 顶部产品意图：“用一句设计意图，找回一张参考。”
2. 紧随其后的自动聚焦自然语言搜索框。
3. 最近添加图片网格，默认展示 8 张并形成完整两排，作为首页唯一的主体内容。
4. 页面底部的次级未解析队列模块；展示等待、解析中、需重试和整体进度，但视觉强度低于图片网格。

首页不展示最近项目、素材数量、标签数量等辅助模块或数据仪表盘。

## 3. 核心端到端流程

### 3.1 首次启动

~~~mermaid
flowchart TD
    A["首次启动桌面端"] --> B["说明本地存储与两级文件夹结构"]
    B --> B2{"建立方式"}
    B2 -->|"创建新库"| C["用户选择素材库根目录"]
    B2 -->|"连接已有库"| C
    B2 -->|"移动现有库"| C
    C --> D{"读取、写入、建目录权限是否通过"}
    D -- "否" --> E["说明原因并重新选择"]
    E --> C
    D -- "是" --> E2["预览根目录 / 项目类型 / 项目名称"]
    E2 --> F["保存根目录并建立索引"]
    F --> G["检查插件离线暂存队列"]
    G --> H["进入首页并聚焦搜索框"]
~~~

### 3.2 插件在线保存

~~~mermaid
flowchart TD
    A["用户在图片工具条点击 Save"] --> B["读取项目类型与网页标题"]
    B --> C["生成一级项目类型 / 二级项目名称路径"]
    C --> D{"同一项目是否存在相同文件哈希"}
    D -- "是" --> E["提示当前项目已保存"]
    D -- "否" --> F["写入正式素材库目录"]
    F --> G["保存来源与文件元数据"]
    G --> H["进入等待解析队列"]
    H --> I["插件提示已保存"]
~~~

跨项目发现相同哈希时允许保存独立副本，并轻提示其他项目已有副本；AI 解析结果可复用。

### 3.3 桌面端完全退出时保存

~~~mermaid
flowchart TD
    A["插件检测桌面端完全退出"] --> B["进入离线暂存模式"]
    B --> C["下载到浏览器默认下载目录下的固定待导入目录"]
    C --> D["扩展本地保存项目、来源和时间元数据"]
    D --> E{"下载是否成功"}
    E -- "否" --> F["显示真实失败原因与重试"]
    E -- "是" --> G["提示已暂存，打开桌面端后自动整理"]
    G --> H["下次启动桌面端"]
    H --> I["自动导入并移动到正式两级目录"]
    I --> J["去重并进入解析队列"]
~~~

关闭窗口时桌面端继续在系统后台运行，按在线流程保存；只有用户选择“完全退出”才进入离线暂存。

### 3.4 自动 AI 解析

~~~mermaid
stateDiagram-v2
    [*] --> 等待中: 图片保存或导入
    等待中 --> 解析中: 系统自动调度
    解析中 --> 已解析: 描述和至少一个关键词标签生成成功
    解析中 --> 等待重试: 临时失败
    等待重试 --> 解析中: 自动重试，最多2次
    等待重试 --> 解析失败: 已达到重试上限
    解析失败 --> 解析中: 用户单张或批量重试
    已解析 --> [*]: 立即进入可搜索素材库
~~~

规则：

- AI 优先从预设关键词标签词库选择，也可生成自定义关键词标签。
- AI 输出包括图片描述、关键词标签、语义向量、模型版本和解析时间。
- AI 结果无需确认即可参与搜索和筛选，并标明 AI 来源。
- 用户可以随时修改描述、删除标签或新增自定义标签。
- 人工修改自动保存，显示“已保存”；每张图片独立保留最近 50 次编辑历史，可连续撤销。

### 3.5 自然语言搜索

~~~mermaid
flowchart TD
    A["打开首页，搜索框自动聚焦"] --> B["用户输入自然语言"]
    B --> C["显示历史搜索与关键词建议"]
    C --> D["用户按 Enter"]
    D --> E["解析意图并在当前项目/标签范围内检索"]
    E --> F["按语义相关度显示瀑布流"]
    F --> G["卡片显示命中关键词或描述片段"]
    G --> H{"用户点击图片"}
    H -- "是" --> I["右侧打开详情面板"]
    H -- "否" --> J["继续修改查询或筛选"]
~~~

搜索不逐字请求 AI。离线时保留项目、关键词标签和文本匹配，明确提示自然语言语义搜索暂不可用。

### 3.6 项目与关键词标签组合筛选

- 点击项目类型：立即显示该类型全部图片，同时展开二级项目名称。
- 点击项目名称：在当前项目类型内进一步收窄。
- 关键词标签按建筑专业维度分组，并显示关联图片数量。
- 所有预设与自定义标签都可拖动到其他分类；移动只改变用户个人词库中的侧栏展示分类，不改变标签值、图片关联、搜索结果或系统原始语义维度。
- 拖动时高亮可放置分类，放下后显示移动结果和短时间“撤销”；放置目标是分类区域，不是另一个具体标签。
- 多个关键词标签之间采用 AND，图片必须包含全部已选标签。
- 用户提交新的非空自然语言查询时，先清除之前遗留的项目范围和关键词条件，以自然语言检索整个素材库，并给出轻量反馈。
- 自然语言结果出现后，用户再次主动选择项目或关键词时，视为二次精筛并与当前查询采用 AND。
- 所有生效条件显示在结果区顶部，可逐个清除或一键重置。
- 无结果时建议移除限制条件，不自动改成 OR。

### 3.7 图片详情与人工修正

点击图片后从右侧打开详情面板，底层瀑布流与滚动位置保持不变。

面板显示：

- 大图、文件名、尺寸、格式和保存时间。
- 项目类型 / 项目名称路径。
- AI 描述和关键词标签；AI 与人工标签以不同色系区分，不在标签后重复追加来源文字。
- 来源网址、网页标题、网站名与“打开原网页”。
- 解析状态和失败原因。

操作：

- 头图右下角“查看原图”打开全屏查看器；详情关闭按钮与头图右上角对齐。关闭原图后返回当前详情，不丢失滚动与编辑状态。
- 编辑描述；增加、删除或修改关键词标签。
- 新增区仅保留一个输入框：输入时筛选由全素材库聚合出的已有词，点击紧凑候选即可复用；输入新词并提交即可创建，不再使用分隔横线或第二个输入框。
- 标签正文只用于阅读；只有右侧独立“×”按钮执行删除，降低误碰风险。
- 撤销按钮显示剩余可回退步数，允许连续撤销该图片最近 50 次描述和标签编辑。
- 修改项目类型或项目名称，并同步移动真实文件。
- 在文件夹中显示、复制或拖拽图片。
- 移到系统回收站。

### 3.8 批量操作

选中第一张图片后才显示批量工具栏，支持：

- 批量添加或移除关键词标签。
- 批量移动项目类型 / 项目名称，并同步移动真实文件。
- 批量重试解析。
- 批量移到系统回收站。

危险操作二次确认；其余操作执行后提供撤销。部分失败时保留成功结果并逐项说明失败原因。

### 3.9 素材库迁移与切换

设置页提供两个不同操作：

- 移动现有素材库：检查目标空间、权限和冲突后，暂停新保存并迁移全部文件与索引；失败时回滚。
- 连接另一个素材库：切换到另一个完整根目录，不让单个素材库跨越多个根目录。

## 4. 页面状态

### 4.1 HOME-01 首页

| 状态 | 页面表现 | 主操作 |
| --- | --- | --- |
| 首次空库 | 搜索框、采集说明、打开插件入口 | 开始保存图片 |
| 默认 | 产品意图、搜索框聚焦、8 张最近添加、底部次级未解析状态模块 | 输入自然语言 |
| 正在导入暂存 | 顶部显示导入数量和进度，最近添加实时更新 | 继续浏览 |
| 导入部分失败 | 保留成功项，列出失败数量 | 查看失败项 |
| 离线 | 本地内容可用，语义搜索提示不可用 | 使用项目或标签筛选 |

### 4.2 SEARCH-01 搜索结果

| 状态 | 页面表现 | 主操作 |
| --- | --- | --- |
| 输入中 | 历史搜索与关键词建议 | Enter 搜索 |
| 理解中 | 保留原查询，显示轻量进度 | 等待或取消 |
| 有结果 | 瀑布流、结果数、匹配原因、可清除条件 | 打开图片 |
| 无结果 | 显示限制最强的条件和调整建议 | 删除条件或改写 |
| 服务失败 | 保留查询和侧栏条件 | 重试或本地筛选 |
| 离线 | 不执行语义查询，保留本地精确匹配 | 项目/标签筛选 |

### 4.3 PARSE-01 未解析

页面顶部显示总体进度和各状态数量；正文默认混合展示所有待处理任务，并可切换等待中、解析中、解析失败筛选。导航角标、首页摘要和页面数量共用“未解析 + 解析中 + 解析失败”的实时口径。

| 状态 | 页面表现 | 主操作 |
| --- | --- | --- |
| 空 | 所有图片已完成解析 | 返回素材库 |
| 等待中 | 图片卡片显示排队位置或等待状态 | 无需操作 |
| 解析中 | 显示当前进度，页面自动更新 | 继续浏览 |
| 部分失败 | 成功项自动离开，失败项保留 | 重试失败项 |
| 全部失败 | 显示共同或逐项原因 | 批量重试 |
| 离线 | 队列暂停，图片仍可浏览 | 等待网络恢复 |

### 4.4 DETAIL-01 图片详情面板

| 状态 | 页面表现 | 主操作 |
| --- | --- | --- |
| 已解析 | 显示描述、标签和来源 | 编辑或复用 |
| 未解析 | 显示等待状态与基础元数据 | 无需操作 |
| 解析失败 | 显示原因 | 重试 |
| 编辑中 | 字段原位编辑 | 自动保存 |
| 保存成功 | 短暂显示已保存和撤销 | 继续编辑 |
| 保存失败 | 保留未提交内容 | 重试 |
| 文件缺失 | 显示最后路径和重新定位入口 | 重新定位 |

### 4.5 LIBRARY-01 素材瀑布流

| 状态 | 页面表现 | 主操作 |
| --- | --- | --- |
| 默认 | 瀑布流与当前范围 | 浏览 |
| 筛选中 | 顶部显示全部生效条件 | 清除或继续叠加 |
| 多选 | 显示批量工具栏和已选数量 | 批量操作 |
| 无结果 | 展示造成限制的条件 | 放宽筛选 |
| 文件操作部分失败 | 成功项保持完成，失败项保留选中 | 查看失败原因 |

### 4.6 SETTINGS-01 素材库设置

| 状态 | 页面表现 | 主操作 |
| --- | --- | --- |
| 正常 | 当前根目录、权限和占用空间 | 移动或连接其他库 |
| 检查中 | 检查目标权限、空间和冲突 | 等待 |
| 迁移中 | 总进度与暂停保存提示 | 等待或取消 |
| 迁移失败 | 原素材库仍可用，显示原因 | 重试或换目录 |
| 目标不可用 | 不切换当前素材库 | 重新选择 |

## 5. 文件与数据一致性规则

- 修改项目归属必须同步移动真实文件；失败时应用状态不更新。
- “移到回收站”同时移除素材库索引，并提供短时间撤销。
- 同一项目内相同文件哈希不重复保存；不同项目允许独立副本。
- 未解析、搜索结果和关键词标签均为逻辑视图，不额外复制图片。
- 来源元数据不被 AI 覆盖；网页失效后仍保留历史网址与标题。
- 用户修改后的描述或关键词标签优先于 AI 结果。

## 6. 低保真原型验收任务

1. 首次选择素材库目录并进入首页。
2. 从插件在线保存一张图片，并观察其自动进入解析队列。
3. 模拟桌面端完全退出后保存，再启动桌面端完成自动导入。
4. 用自然语言找到目标图片，并理解其匹配原因。
5. 组合“文化建筑 + 清水混凝土 + 自然采光”筛选图片。
6. 在右侧详情面板修正描述和关键词标签，确认自动保存与撤销。
7. 批量移动图片到另一项目，并验证真实文件路径同步改变。
8. 将图片移到系统回收站并撤销。

完成标准：用户无需讲解即可完成至少 7/8 个任务；核心任务 2、4、6 必须全部成功。
