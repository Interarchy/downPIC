# AI 建筑灵感素材库｜用户流程与页面状态 v1.0

> 文档状态：待交互评审  
> 上游文档：[SPEC.md](./SPEC.md)、[PRD.md](./PRD.md)  
> 桌面端已确认流程：[DESKTOP_UX_FLOW.md](./DESKTOP_UX_FLOW.md)  
> 目标平台：Windows / Chrome  
> 文档用途：低保真线框图、可点击原型、技术状态设计和测试用例的共同输入

## 0. 文档目标

本文档定义：

- 用户从安装到复用素材的完整路径
- 浏览器插件与 Windows 桌面素材管理应用之间的跳转关系
- 每个核心页面的默认、空、加载、成功、失败和离线状态
- 图片、AI 任务、关键词、搜索和文件监听的关键状态流转
- 异常情况下用户可以采取的下一步操作

本文档不定义视觉风格、高保真组件样式和具体技术实现。

## 1. 角色与系统边界

### 1.1 用户角色

MVP 只有一个业务角色：受邀设计师。

受邀设计师可以：

- 注册并登录基础账号
- 选择或自定义项目类型
- 由网页标题自动生成项目名称
- 创建收藏夹
- 主动逐张采集网页图片
- 导入本地图片并绑定监听文件夹
- 手动触发 AI 分析
- 校正和管理关键词
- 使用关键词、分类筛选和自然语言搜索
- 打开、复制、拖拽或返回图片来源

MVP 不存在管理员后台、团队成员或访客角色。

### 1.2 系统组成

| 系统 | 用户可见职责 |
| --- | --- |
| 浏览器插件 | 当前项目类型、网页项目名、图片保存功能状态、单图保存反馈、连接状态 |
| Windows 桌面素材管理应用 | 独立应用窗口、图库、未解析、项目、收藏夹、AI、关键词和搜索 |
| 桌面应用内部核心模块 | 文件保存、数据库、索引、目录监听、后台任务和插件通信 |
| 云端账号服务 | 邀请、登录、AI 额度 |
| 云端 AI 服务 | 图片理解、描述、关键词建议、查询理解和文本向量 |

### 1.3 在线与离线边界

| 能力 | 在线 | 离线 |
| --- | --- | --- |
| 浏览本地图库 | 可用 | 可用 |
| 项目和收藏夹管理 | 可用 | 可用 |
| 关键词人工编辑 | 可用 | 可用 |
| 关键词与分类筛选 | 可用 | 可用 |
| 网页图片采集 | 可用 | 取决于网页是否可访问 |
| AI 图片分析 | 可用 | 不可用 |
| 自然语言混合搜索 | 可用 | 降级为关键词和分类筛选 |
| 登录与额度刷新 | 可用 | 显示最近状态，不可刷新 |

## 2. 页面地图

~~~mermaid
flowchart TD
    A["首次启动"] --> B["邀请码注册 / 登录"]
    B --> C["隐私与版权确认"]
    C --> D["桌面应用与素材目录检查"]
    D --> E["桌面应用首页"]

    E --> F["全局素材库"]
    E --> Y["未解析"]
    E --> G["项目"]
    E --> H["收藏夹"]
    E --> I["AI 任务"]
    E --> J["关键词管理"]
    E --> K["导入与监听文件夹"]
    E --> L["设置"]

    F --> M["图片详情"]
    G --> N["项目详情"]
    H --> O["收藏夹详情"]
    N --> M
    O --> M

    L --> P["账号与额度"]
    L --> Q["素材目录"]
    L --> R["数据与隐私"]
    L --> S["连接诊断"]
    L --> T["版权说明"]

    U["浏览器插件"] --> V["选择项目类型"]
    V --> W["图片保存功能"]
    W --> Z["网页标题生成项目名"]
    Z --> X["单图保存反馈"]
    X --> E
~~~

## 3. 核心端到端流程

### 3.1 主成功路径

~~~mermaid
flowchart LR
    A["受邀注册"] --> B["选择项目类型"]
    B --> C["开启图片保存功能"]
    C --> D["点击目标图片"]
    D --> E["网页标题生成项目名"]
    E --> F["保存到两级项目文件夹"]
    F --> G["进入全局库和未解析"]
    G --> H["系统自动 AI 解析"]
    H --> I["需要时修正描述或关键词标签"]
    I --> J["自然语言搜索"]
    J --> K["调整查询理解"]
    K --> L["找到图片"]
    L --> M["复制到项目 / 打开 / 复制 / 拖拽"]
~~~

### 3.2 核心闭环判断

用户必须完成以下动作，才算跑通完整产品闭环：

1. 至少保存或导入一张图片。
2. 观察图片自动进入解析队列并完成解析。
3. 至少人工修改一次描述或关键词标签。
4. 使用搜索找到一张图片。
5. 对搜索结果执行一次复用动作。

## 4. 全局导航与通用交互规则

### 4.1 桌面素材管理应用主导航

固定一级入口：

1. 首页
2. 全局素材库
3. 未解析
4. 按项目浏览
5. 按关键词浏览
6. 收藏夹
7. AI 任务
8. 关键词管理
9. 导入与监听
10. 设置

### 4.2 双路径侧栏浏览

左侧栏包含两个主要浏览分区：

- 按项目浏览：展开“项目类型 / 项目名称”，查看实际文件夹中的图片。
- 按关键词标签浏览：展开空间、材料、构造、形态、风格、氛围和其他分组，查看关键词关联图片。

交互规则：

- 关键词分区顶部提供侧栏内关键词搜索。
- 项目和关键词均显示关联图片数量。
- 项目范围与关键词可分别选择，也可以组合使用。
- 多个关键词之间固定采用“同时满足”，并继续与项目范围和自然语言查询组合。
- 当前生效条件在内容区顶部显示，避免过滤状态隐藏在侧栏中。
- 清除项目范围不会清除关键词；清除关键词不会清除项目范围。
- 未解析图片没有关键词时，仍可通过全局、未解析和项目路径找到。

### 4.3 全局状态区

所有主页面均可访问以下状态：

- 插件与桌面应用连接状态
- 桌面后台任务状态
- 在线/离线状态
- 当前登录账号
- AI 剩余额度
- 正在进行的 AI 任务数量
- 全局通知

### 4.4 返回规则

- 图片详情关闭后返回进入详情前的列表、筛选和滚动位置。
- 从搜索结果进入详情再返回时，保留查询和结果排序。
- 从项目或收藏夹进入详情再返回时，保留当前项目上下文。
- 从插件打开桌面应用时，默认进入当前项目或最近保存图片。

### 4.5 通知层级

| 类型 | 用途 | 示例 |
| --- | --- | --- |
| 即时轻提示 | 无需用户处理的成功反馈 | 图片已保存 |
| 页面内提示 | 当前页面可继续操作的异常 | 部分图片分析失败 |
| 阻断对话框 | 不确认就可能产生误操作 | 批量修改关键词 |
| 全局横幅 | 影响多个页面的系统状态 | 插件连接断开、当前离线 |

### 4.6 空状态规则

空状态必须同时包含：

- 当前为什么没有内容
- 用户下一步可以做什么
- 一个主要行动入口

禁止仅显示“暂无数据”。

### 4.7 桌面应用运行状态

| 状态 | 插件采集 | 文件夹监听 | AI 后台任务 | 用户反馈 |
| --- | --- | --- | --- | --- |
| 应用窗口打开 | 可用 | 可用 | 可用 | 正常显示 |
| 最小化到后台 | 可用 | 可用 | 可用 | 系统托盘显示运行状态 |
| 窗口关闭但继续后台 | 可用 | 可用 | 可用 | 插件显示“后台运行” |
| 完全退出 | 可用，进入浏览器离线暂存 | 停止 | 暂停或取消 | 插件显示“桌面端未运行 · 可暂存” |
| 应用更新中 | 暂时不可用 | 视更新状态暂停 | 安全暂停 | 显示更新进度 |

关闭桌面窗口且仍有 AI 或目录监听任务时，必须让用户选择“继续后台运行”或“完全退出”，不能无提示终止任务。

## 5. 流程 A：首次启动与注册

### 5.1 流程图

~~~mermaid
flowchart TD
    A["打开 Windows 桌面应用"] --> B{"是否首次启动"}
    B -- "否" --> J["进入首页"]
    B -- "是" --> C["输入邀请码"]
    C --> D{"邀请码有效"}
    D -- "否" --> E["显示原因并允许重试"]
    E --> C
    D -- "是" --> F["注册或登录"]
    F --> G["版权与合法使用确认"]
    G --> H["详细统计授权"]
    H --> I["初始化桌面核心模块与素材目录"]
    I --> K{"检查通过"}
    K -- "是" --> J
    K -- "否" --> L["进入连接诊断"]
    L --> I
~~~

### 5.2 页面：欢迎与邀请码

页面 ID：ONB-01

| 状态 | 页面表现 | 主操作 | 次操作 |
| --- | --- | --- | --- |
| 默认 | 产品价值、邀请码输入 | 验证邀请码 | 查看隐私说明 |
| 验证中 | 输入锁定、显示进度 | 无 | 取消 |
| 无效 | 显示邀请码无效 | 重新输入 | 获取帮助 |
| 过期 | 显示邀请码已过期 | 更换邀请码 | 获取帮助 |
| 网络失败 | 保留输入内容 | 重试 | 查看网络状态 |
| 成功 | 自动进入注册 | 继续 | 无 |

### 5.3 页面：账号注册/登录

页面 ID：ONB-02

关键状态：

- 未填写
- 格式错误
- 提交中
- 账号已存在
- 登录失败
- 注册成功
- 离线不可用

规则：

- 登录失败不得清理本地素材。
- 用户退出账号后，本地图库仍存在，但 AI 与额度能力不可用。

### 5.4 页面：版权确认

页面 ID：ONB-03

必须说明：

- 用户只应采集其有权访问和合法使用的图片。
- 产品不授予图片版权。
- 产品不会绕过网站限制。
- 来源信息只用于追溯，不代表获得授权。

未确认前不能进入网页采集功能。

### 5.5 页面：数据与隐私授权

页面 ID：ONB-04

必须将以下授权分开：

- 核心服务所必需的数据处理
- 详细行为与搜索词统计

状态：

- 未选择
- 同意详细统计
- 拒绝详细统计
- 查看完整说明

规则：

- 拒绝详细统计不影响核心功能。
- 用户后续可以在设置中修改决定。

### 5.6 页面：本地环境检查

页面 ID：ONB-05

检查项：

- 桌面应用内部核心模块运行
- 插件连接
- 素材目录可写
- 可用磁盘空间
- 云端账号与 AI 服务连接

结果状态：

- 全部通过
- 部分能力不可用
- 阻断错误

## 6. 流程 B：选择项目类型并自动生成项目名称

### 6.1 流程图

~~~mermaid
flowchart TD
    A["打开插件"] --> B["查看当前项目类型"]
    B --> C{"使用已有类型"}
    C -- "是" --> D["选择预设或个人类型"]
    C -- "否" --> E["新建自定义项目类型"]
    E --> F{"去除首尾空白后名称有效"}
    F -- "否" --> G["显示校验错误"]
    G --> E
    F -- "是" --> D
    D --> H["开启图片保存功能"]
    H --> I["首次点击图片悬浮工具条中的保存"]
    I --> J["读取并清理网页标题"]
    J --> K{"标题有效"}
    K -- "否" --> L["使用网站名＋采集日期"]
    K -- "是" --> M["生成项目名称"]
    L --> N["创建两级项目目录"]
    M --> N
    N --> O["桌面应用记录项目名称，插件仅保留项目类型"]
~~~

### 6.2 页面：项目类型选择

页面 ID：LIB-TYPE-01

| 状态 | 页面内容 | 主操作 |
| --- | --- | --- |
| 空 | 尚无可用项目类型 | 新建项目类型 |
| 有内容 | 预设类型、个人类型、最近使用 | 选择类型 |
| 加载 | 类型占位 | 无 |
| 读取失败 | 错误原因 | 重试 |

预设示例包括办公建筑、学校建筑、文化建筑。用户可新增、重命名和归档个人项目类型。

### 6.3 页面：自动项目名称

页面 ID：LIB-PRJ-01

该页面属于桌面应用，不在插件中展示或编辑。

字段：

- 项目类型：来自插件当前选择
- 项目名称：默认来自网页标题，可编辑
- 来源网址：用于识别同一网页和处理同名冲突
- 文件夹预览：展示最终两级路径

状态：

- 尚未点击图片，项目名待生成
- 已读取网页标题
- 使用备用名称
- 用户已修改名称
- 名称冲突，自动消歧
- 创建文件夹中
- 创建成功
- 创建失败

### 6.4 目录与当前状态规则

- 同一时间只有一个当前项目类型。
- 当前项目类型保持到用户主动切换。
- 当前项目类型是物理存储依据，AI 不得自动修改。
- 每个网页拥有当前类型下的一个自动项目名称。
- 同一网页后续采集复用同一二级项目文件夹。
- 用户修改项目名称后，同一网页继续使用修改后的名称。
- 修改项目类型时移动整个二级项目文件夹。
- 修改项目名称时同步重命名二级项目文件夹。
- 项目类型被归档或不可用时，图片保存功能自动关闭并要求重新选择。
- 收藏夹不作为物理保存目标，只建立逻辑引用。

## 7. 流程 C：网页单图采集

### 7.1 流程图

~~~mermaid
flowchart TD
    A["打开插件"] --> B{"桌面应用已连接"}
    B -- "否" --> C["显示未运行并进入离线暂存模式"]
    B -- "是" --> D{"已选择项目类型"}
    C --> D
    D -- "否" --> E["选择或新建项目类型"]
    D -- "是" --> F["开启图片保存功能"]
    E --> F
    F --> G["鼠标经过可采集内容图片"]
    G --> H["显示悬浮工具条并继承默认项目类型"]
    H --> I{"是否修改项目类型"}
    I -- "是" --> I1["选择预设或直接输入自定义类型"]
    I1 --> I2["暂存当前图片的类型选择"]
    I -- "否" --> J0["沿用当前默认类型"]
    I2 --> J1["点击工具条中的 Save"]
    J0 --> J1
    J1 --> J{"格式支持"}
    J -- "否" --> K["提示格式不支持"]
    J -- "是" --> L["解析最佳可用资源"]
    L --> M{"资源可访问"}
    M -- "否" --> N["回退或显示访问限制"]
    M -- "是" --> O["读取网页标题并确定项目名"]
    O --> P["创建或复用两级项目文件夹"]
    P --> P1{"桌面应用运行中"}
    P1 -- "是" --> Q["下载并保存到当前项目文件夹"]
    P1 -- "否" --> Q0["下载到固定待导入目录并记录元数据"]
    Q0 --> Q2["显示已暂存；下次启动自动整理"]
    Q --> Q1["将本次类型更新为全局默认值"]
    Q1 --> R{"当前项目内完全重复"}
    R -- "是" --> S["显示当前项目已存在"]
    R -- "否" --> T["创建图片记录并进入未解析"]
    T --> U["显示保存成功"]
~~~

### 7.2 插件主面板

页面 ID：EXT-01

固定内容：

- 图片保存功能开关
- 项目类型选择
- 始终可见的自定义项目类型输入框
- 设为默认按钮
- 自定义名称去除首尾空白后为空时，禁止创建类型和保存图片，并在输入框附近提示“请输入项目类型”。
- 打开桌面应用

轻量状态：

- 桌面应用连接状态点
- 保存成功、重复或失败的临时消息

明确不显示：

- 网页项目名称和文件夹路径
- AI 额度和任务
- 最近保存历史
- 账号与登录信息
- 图库、关键词、搜索和设置

页面状态：

| 状态 | 图片保存功能 | 页面表现 | 主操作 |
| --- | --- | --- | --- |
| 完全退出 | 可用 | 进入离线暂存模式 | 保存并等待下次自动导入 |
| 未登录 | 禁用 | 账号未登录 | 登录 |
| 未确认版权 | 禁用 | 需完成版权确认 | 查看并确认 |
| 未选择类型 | 禁用 | 尚未设置项目类型 | 选择类型 |
| 就绪 | 可用 | 显示已选项目类型 | 开启图片保存功能 |
| 已开启 | 开启 | 显示图片保存功能已开启 | 关闭图片保存功能 |
| 离线 | 可用 | AI 延迟到桌面端启动后执行 | 继续暂存图片 |

### 7.2.1 极简交互规则

- 插件主要界面只有“图片保存功能开关”和“项目类型”两块。
- “打开桌面应用”作为唯一辅助按钮。
- 预设类型下拉框与自定义输入框始终处于第一层级，输入框不再隐藏在下拉选项后。
- 两者互斥：输入自定义内容时取消预设选择，选择预设或“我的类型”时清空自定义输入。
- 插件记住最近一次有效项目类型。
- 主面板中的项目类型是全局默认值，与网页悬浮工具条双向同步。
- 未选择有效类型时不能开启图片保存功能。
- 网页项目名称自动生成，查看和修改均在桌面应用完成。
- 保存消息短暂出现后自动消失，不形成历史列表。

### 7.3 网页采集覆盖状态

页面 ID：EXT-02

仅在图片保存功能开启且鼠标经过可保存的内容图片时显示悬浮工具条。工具条位于图片内部上方居中，始终显示预设类型、自定义输入和“Save”三个第一层级控件。

| 状态 | 工具条表现 | 用户操作 |
| --- | --- | --- |
| 隐藏 | 图片保存功能关闭或鼠标未经过可保存图片 | 正常浏览网页 |
| 就绪 | 显示当前默认项目类型和“保存” | 直接保存或切换类型 |
| 自定义类型 | 输入框保持可见并取消预设选择 | 输入有效名称并保存 |
| 保存中 | 锁定重复点击，显示获取资源进度 | 等待 |
| 已保存 | 短暂显示“已下载并保存到当前项目类型” | 继续浏览 |
| 已存在 | 显示“当前项目已存在” | 查看图片或继续浏览 |
| 失败 | 显示简短原因和重试入口 | 重试或跳过 |
| 已离线暂存 | 显示“已暂存，打开桌面端后自动整理” | 继续浏览或打开桌面端 |
| 格式不支持 | 显示不可采集状态 | 关闭或跳过 |
| 网站限制 | 显示“网站限制了原图访问”，明确未保存 | 在有权限的页面重试或跳过 |
| 原图失效 | 显示“原图地址已失效”，明确未保存 | 跳过 |

项目类型同步规则：

- 工具条初始值始终继承当前全局默认类型。
- 在工具条中切换或输入类型只形成当前待保存选择；点击“Save”后才写回主面板全局默认值。
- 弹窗中的“设为默认”只更新之后出现的工具条，不保存图片。
- 新类型提交后同时用于当前图片和之后出现的所有工具条，直到用户再次修改。
- 自定义类型提交成功后进入个人项目类型词库。
- 网页图片本身保持原有点击行为；只有点击工具条中的“保存”才触发采集。

### 7.4 保存反馈

| 结果 | 反馈文案含义 | 用户下一步 |
| --- | --- | --- |
| 保存成功 | 图片已进入两级项目文件夹、全局库和未解析 | 继续浏览 |
| 当前项目已存在 | 同一项目内不重复保存 | 查看图片或继续 |
| 跨项目已有副本 | 当前项目仍保存独立副本 | 继续浏览 |
| 格式不支持 | 当前资源不是 JPEG/PNG/WebP | 关闭提示 |
| 无法访问原图 | 已尝试最佳可用资源 | 重试或跳过 |
| 网站限制 | 不绕过当前网站限制 | 返回正常浏览 |
| 本地写入失败 | 未保存文件 | 打开诊断 |
| 磁盘空间不足 | 未保存文件 | 更换目录或清理空间 |

### 7.5 防误操作规则

- 图片保存功能关闭时完全保留网页原有点击行为。
- 图片保存功能开启后必须有持续可见的状态提示。
- 按 Esc 或使用插件开关可关闭图片保存功能。
- 同一图片保存中禁止再次触发。
- 采集失败不得导致网页跳转或刷新。
- 悬浮工具条不得大面积遮挡图片主体，且不得劫持图片自身点击、链接或拖拽行为。

## 8. 流程 D：本地图片导入

### 8.1 流程图

~~~mermaid
flowchart TD
    A["打开导入页面"] --> B["选择单张或多张文件"]
    B --> C["检查格式与读取权限"]
    C --> D["计算完全重复"]
    D --> E["复制到产品素材目录"]
    E --> F["选择关联项目或收藏夹"]
    F --> G["生成导入结果"]
    G --> H{"是否存在失败项"}
    H -- "是" --> I["查看原因并重试"]
    H -- "否" --> J["打开导入后的图片"]
~~~

### 8.2 页面：文件导入

页面 ID：IMP-01

状态：

- 初始选择
- 正在扫描
- 等待确认
- 正在复制
- 完成
- 部分完成
- 全部失败
- 权限不足
- 空间不足

结果必须区分：

- 成功导入
- 完全重复
- 格式不支持
- 文件损坏
- 无读取权限
- 无写入空间

### 8.3 导入规则

- 只接受 JPEG、PNG、WebP。
- 原文件不会被移动或删除。
- 成功导入后使用产品管理副本。
- 完全重复文件不再复制，但可以关联到新目标。
- 用户取消导入后，已完成复制的图片保留，未开始项停止。

## 9. 流程 E：绑定监听文件夹

### 9.1 流程图

~~~mermaid
flowchart TD
    A["选择监听文件夹"] --> B["检查访问权限"]
    B --> C{"权限有效"}
    C -- "否" --> D["提示重新授权"]
    C -- "是" --> E["首次扫描"]
    E --> F["显示发现、重复和失败数量"]
    F --> G["确认导入"]
    G --> H["复制图片到产品目录"]
    H --> I["进入持续监听"]
    I --> J{"检测到变化"}
    J -- "新增" --> K["导入新图片"]
    J -- "源文件删除" --> L["保留产品副本"]
    J -- "已忽略文件再次出现" --> M["不重新导入"]
~~~

### 9.2 页面：监听文件夹

页面 ID：IMP-02

列表项显示：

- 文件夹路径
- 状态
- 最近扫描时间
- 最近新增数量
- 错误数量

文件夹状态：

- 正常监听
- 首次扫描中
- 暂停
- 路径不可用
- 权限丢失
- 扫描失败
- 已移除

用户操作：

- 添加文件夹
- 暂停/继续
- 立即扫描
- 重新授权
- 移除监听

移除监听不删除已经导入的产品管理副本。

## 10. 流程 F：未解析视图

### 10.1 状态流

~~~mermaid
stateDiagram-v2
    state "未解析" as unparsed
    state "解析中" as processing
    state "已解析" as parsed
    state "解析失败" as failed
    [*] --> unparsed: 图片采集或导入
    unparsed --> processing: 系统自动调度
    processing --> parsed: 描述和关键词均生成
    processing --> failed: 请求失败或结果不完整
    processing --> processing: 临时失败后自动重试，最多2次
    failed --> processing: 用户单张或批量重试
    parsed --> unparsed: 用户清除解析结果
~~~

### 10.2 页面：未解析

页面 ID：PARSE-01

“未解析”是跨项目的逻辑视图，不建立新文件夹，也不移动或复制图片。

固定内容：

- 未解析总数
- 解析中数量
- 解析失败数量
- 项目类型、项目名称和采集时间筛选
- 等待中、解析中和解析失败三个区段
- 单张和批量重试入口

页面状态：

| 状态 | 页面表现 | 主操作 |
| --- | --- | --- |
| 空 | 所有图片均已解析 | 返回全局图库 |
| 有内容 | 按状态分区展示未解析图片 | 自动处理，无需操作 |
| 处理中 | 显示正在解析图片和进度 | 查看 AI 任务 |
| 部分失败 | 成功项离开列表，失败项保留 | 重试失败项 |
| 全部失败 | 显示共同或逐项失败原因 | 重试 |
| 离线 | 图片可浏览，AI 入口禁用 | 网络恢复后重试 |

### 10.3 进入与退出规则

- 新保存和新导入图片自动进入未解析并排队。
- 系统调度后状态切换为解析中，但仍可在该页面查看。
- 同时具有非空图片描述和至少一个关键词时切换为已解析并离开列表。
- 临时失败自动重试 2 次；仍失败、描述为空或没有关键词时切换为解析失败并继续保留。
- 清除 AI 解析结果后重新进入未解析。
- 页面一级导航持续显示未解析和失败数量。

## 11. 流程 G：自动 AI 解析与人工重试

### 11.1 流程图

~~~mermaid
flowchart TD
    A["新图片自动进入解析队列"] --> B["系统统计等待中和已解析图片"]
    B --> C["显示图片数与预计额度"]
    C --> F{"在线且额度足够"}
    F -- "否" --> G["显示网络或额度问题"]
    F -- "是" --> H["创建 AI 任务"]
    H --> I["排队中"]
    I --> J["处理中"]
    J --> K{"任务结果"}
    K -- "全部成功" --> L["写入描述、关键词标签和向量，立即可搜索"]
    K -- "部分成功" --> M["保留成功结果并列出失败图片"]
    K -- "失败" --> N["显示失败原因"]
    M --> O["单独重试失败项"]
    N --> O
~~~

### 11.2 人工重试与重新解析入口

失败重试或主动重新解析入口出现在：

- 图片详情
- 图片多选工具栏
- 未解析视图
- 项目详情
- 收藏夹详情
- AI 任务页

### 11.3 主动重新解析确认对话框

页面 ID：AI-01

显示：

- 分析范围
- 图片总数
- 已分析数量
- 本次重新分析数量
- 预计额度消耗
- 当前剩余额度

状态：

- 可提交
- 额度不足
- 当前离线
- 包含处理中图片
- 没有可分析图片

### 11.4 AI 任务中心

页面 ID：AI-02

任务列表分组：

- 处理中
- 已完成
- 部分成功
- 失败

每个任务显示：

- 创建时间
- 分析范围
- 图片总数
- 成功/失败数量
- 当前进度
- 消耗额度
- 模型版本

用户操作：

- 查看任务详情
- 取消尚未开始的任务
- 重试失败图片
- 打开分析结果

### 11.5 AI 任务状态机

~~~mermaid
stateDiagram-v2
    state "待提交" as submitted
    state "排队中" as queued
    state "处理中" as processing
    state "成功" as succeeded
    state "部分成功" as partial
    state "失败" as failed
    state "已取消" as cancelled
    [*] --> submitted
    submitted --> queued: 系统自动创建或用户确认重新解析
    submitted --> cancelled: 用户取消
    queued --> processing: 服务开始
    queued --> cancelled: 允许取消
    processing --> succeeded: 全部图片成功
    processing --> partial: 部分图片失败
    processing --> failed: 没有有效结果
    partial --> queued: 重试失败项
    failed --> queued: 用户重试
    succeeded --> [*]
    cancelled --> [*]
~~~

### 11.6 异常规则

- 单张失败不得回滚同批次的成功结果。
- 用户人工修改的关键词标签在重新分析时保持不变。
- 用户删除的图片—关键词组合不得被普通重试反复推荐。
- 缺少描述或关键词的结果按解析失败处理，并留在未解析视图。
- AI 建议项目类型与当前物理类型不一致时只显示提示，用户明确确认迁移前不移动目录。
- 断网时任务停留在可恢复状态。

## 12. 流程 H：关键词标签校正与词库管理

### 12.1 单张关键词校正

~~~mermaid
flowchart TD
    A["打开右侧图片详情"] --> B["查看 AI 描述与关键词标签"]
    B --> C{"是否需要修正"}
    C -- "否" --> D["关闭面板，AI 结果继续可搜索"]
    C -- "是" --> E["编辑描述或增删关键词标签"]
    E --> F["自动保存并更新搜索索引"]
    F --> G["显示已保存与短时间撤销"]
~~~

### 12.2 批量关键词校正

页面 ID：TAG-01

流程：

1. 用户在图库中多选图片。
2. 选择“批量编辑关键词”。
3. 查看所选数量。
4. 选择增加或删除关键词。
5. 系统展示影响范围。
6. 用户确认。
7. 更新全部图片与搜索索引。

状态：

- 无选择
- 已选择
- 正在应用
- 全部成功
- 部分失败

### 12.3 关键词标签来源状态机

~~~mermaid
stateDiagram-v2
    state "AI 生成" as aiSuggested
    state "用户修改" as userConfirmed
    state "人工创建" as userCreated
    state "人工删除记录" as removed
    [*] --> aiSuggested: 分析生成
    aiSuggested --> userConfirmed: 修改
    aiSuggested --> removed: 用户删除
    [*] --> userCreated: 用户添加
    userConfirmed --> removed: 再次删除
    userCreated --> removed: 用户删除
~~~

### 12.4 关键词管理页

页面 ID：TAG-02

页面分区：

- 空间
- 材料
- 构造
- 形态
- 风格
- 氛围
- 其他
- 自定义关键词
- 已归档关键词

单个关键词显示：

- 名称
- 维度
- 定义
- 参考图片数量
- 已关联图片数量
- 来源
- 当前状态

用户操作：

- 新建
- 编辑名称和定义
- 添加参考图片
- 归档
- 恢复

### 12.5 归档流程

~~~mermaid
flowchart TD
    A["用户选择归档关键词"] --> B{"关键词是否已被使用"}
    B -- "否" --> C["直接归档"]
    B -- "是" --> D["显示关联图片数量"]
    D --> E["确认归档"]
    E --> F["历史关联保留"]
    F --> G["停止用于新图片和 AI 推荐"]
    C --> G
~~~

## 13. 流程 I：统一搜索与查询解释

### 13.1 搜索主流程

~~~mermaid
flowchart TD
    P["可选：从左栏选择项目范围"] --> R["共享结果区"]
    Q["可选：从左栏选择一个或多个关键词"] --> R
    R --> A{"是否输入顶部自然语言"}
    A -- "否" --> L["按当前项目和关键词过滤"]
    A -- "是" --> B{"当前是否在线"}
    B -- "否" --> C["保留侧栏条件并执行本地关键词搜索"]
    B -- "是" --> D["解析自然语言意图"]
    D --> E["生成可编辑查询条件"]
    E --> F["并行检索项目类型、关键词、描述和语义向量"]
    L --> G["融合排序"]
    C --> G
    F --> G["融合排序"]
    G --> H["展示结果与匹配原因"]
    H --> I{"用户是否满意"}
    I -- "是" --> J["打开或复用图片"]
    I -- "否" --> K["编辑条件或改写查询"]
    K --> A
~~~

### 13.2 首页/搜索页

页面 ID：SRCH-01

固定区域：

- 统一搜索框
- 输入时的历史搜索和关键词建议；按 Enter 执行自然语言搜索
- 查询理解条件
- 左侧按项目浏览
- 左侧按关键词浏览
- 当前项目范围、关键词和自然语言条件
- 结果数量与排序说明
- 瀑布流结果

页面状态：

| 状态 | 页面表现 | 主操作 |
| --- | --- | --- |
| 初始且图库为空 | 产品价值和采集/导入入口 | 开始采集或导入 |
| 初始且有图片 | 搜索框自动聚焦，并显示最近添加、未解析摘要和最近项目 | 输入搜索 |
| 输入中 | 保留原始文本 | 提交 |
| 解析中 | 显示查询正在理解 | 等待或取消 |
| 检索中 | 保留搜索框和条件 | 等待 |
| 有结果 | 展示结果与匹配原因 | 打开图片 |
| 无结果 | 展示已使用条件和调整建议 | 删除条件或改写 |
| 服务降级 | 展示关键词和分类筛选结果 | 继续或稍后重试 |
| 离线 | 明确自然语言不可用 | 使用本地搜索 |
| 搜索失败 | 保留查询文本 | 重试 |

### 13.3 查询理解条件

条件来源可以是：

- 项目类型
- 空间类型
- 建筑风格
- 主要材料
- 用户自定义概念
- 普通关键词

用户可以：

- 删除条件
- 替换为其他关键词
- 增加筛选条件
- 保留原始查询重新搜索

规则：

- 查询条件是对 AI 理解的解释，不覆盖用户原始输入。
- 调整条件后无需重新输入完整句子。
- 解析失败不能阻止关键词搜索。
- 未选择左侧条件时，自然语言搜索覆盖整个素材库。
- 已选择项目范围或关键词时，自然语言只在当前范围内继续检索。
- 项目、关键词和自然语言三类条件均可独立清除。

### 13.4 搜索结果卡片

每张卡片显示：

- 图片缩略图
- 主要关键词
- AI/人工关键词来源
- 所属项目或收藏夹提示
- 主要匹配原因

卡片交互：

- 打开详情
- 快速加入当前项目
- 打开来源

### 13.5 无结果恢复

无结果页必须提供：

- 当前原始查询
- 当前解析条件
- 哪些条件限制最强
- 删除某一条件
- 清空全部条件
- 改用关键词搜索

不得仅显示“没有结果”。

## 14. 流程 J：查看和复用图片

### 14.1 图片详情流程

~~~mermaid
flowchart TD
    A["打开图片详情"] --> B{"原文件是否存在"}
    B -- "否" --> C["显示文件缺失状态"]
    B -- "是" --> D["显示大图和完整信息"]
    D --> E{"用户操作"}
    E -- "复制到其他项目" --> F["创建独立文件副本"]
    E -- "加入收藏夹" --> M["建立逻辑引用"]
    E -- "打开来源" --> G["打开来源页面"]
    E -- "文件夹中定位" --> H["定位本地文件"]
    E -- "复制" --> I["复制图片"]
    E -- "拖拽" --> J["拖入其他软件"]
    E -- "编辑关键词" --> K["更新关键词与索引"]
    E -- "移到回收站" --> L["进入回收站确认"]
~~~

### 14.2 图片详情页

页面 ID：ASSET-01

内容区域：

- 大图预览
- 关键词
- 图片描述
- AI 状态
- 所属项目类型、项目名称和收藏夹
- 文件信息
- 来源信息

状态：

- 正常
- 图片加载中
- 缩略图可用但原文件缺失
- 来源缺失
- AI 未分析
- AI 处理中
- AI 失败
- 离线

### 14.3 复用动作反馈

| 动作 | 成功反馈 | 失败处理 |
| --- | --- | --- |
| 复制到其他项目 | 已创建独立副本 | 保留详情并重试 |
| 加入收藏夹 | 已建立逻辑引用 | 保留详情并重试 |
| 打开来源 | 新标签页打开 | URL 缺失或失效提示 |
| 文件夹中定位 | 打开资源管理器 | 文件缺失提示 |
| 复制 | 已复制 | 权限或文件错误提示 |
| 拖拽 | 由目标软件接收 | 保持原图不变 |

## 15. 流程 K：移到系统回收站

### 15.1 流程图

~~~mermaid
flowchart TD
    A["用户选择移到回收站"] --> B["说明本地文件与素材库索引都将移除"]
    B --> C{"用户确认"}
    C -- "否" --> D["返回原页面"]
    C -- "是" --> E["将本地文件移到系统回收站"]
    E --> F{"文件操作是否成功"}
    F -- "否" --> G["保留文件和索引并显示原因"]
    F -- "是" --> H["移除索引和关联关系"]
    H --> I["完成并提供短时间撤销"]
~~~

### 15.2 回收站确认

页面 ID：ASSET-02

必须显示：

- 将从图库、项目和收藏夹移除
- 关键词、描述和搜索索引将删除
- 磁盘中的图片文件将进入系统回收站
- 完成后可在短时间内撤销

批量操作时必须显示图片数量。

## 16. 流程 L：设置与连接诊断

### 16.1 设置首页

页面 ID：SET-01

入口：

- 账号与 AI 额度
- 素材目录
- 数据与隐私
- 插件与桌面应用连接
- 监听文件夹
- 版权说明

### 16.2 素材目录状态

页面 ID：SET-02

状态：

- 正常可写
- 空间不足预警
- 无写入权限
- 路径不存在
- 迁移中
- 迁移失败
- 迁移完成

目录迁移属于 P1，但状态需预留。

### 16.3 连接诊断

页面 ID：SET-03

诊断顺序：

1. 桌面应用是否安装和运行
2. 桌面应用内部核心模块是否正常
3. 浏览器插件是否安装
4. 插件是否有必要权限
5. 插件与桌面应用通信是否成功
6. 素材目录是否可写
7. 账号服务是否可达
8. AI 服务是否可达

每项结果：

- 正常
- 警告
- 错误
- 未检查

每个错误必须提供一个明确的下一步操作。

### 16.4 数据与隐私

页面 ID：SET-04

用户可以：

- 查看当前详细统计授权
- 开启或关闭详细统计
- 查看保存的数据类型
- 请求删除 30 天保存期内的数据
- 查看 AI 图片临时处理说明

关闭详细统计后：

- 立即停止后续详细事件上传。
- 不影响桌面端本地素材库和核心功能。
- 已收集数据按用户选择删除或等待到期处理。

## 17. 页面状态总表

| 页面 | 默认 | 空 | 加载 | 成功 | 部分成功 | 错误 | 离线 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 欢迎/注册 | 有 | 不适用 | 有 | 有 | 不适用 | 有 | 阻断 |
| 素材库首页 | 有 | 有 | 有 | 有 | 不适用 | 有 | 可用 |
| 项目列表 | 有 | 有 | 有 | 有 | 不适用 | 有 | 可用 |
| 项目/收藏夹详情 | 有 | 有 | 有 | 有 | 不适用 | 有 | 可用 |
| 图片详情 | 有 | 不适用 | 有 | 有 | 有 | 有 | 可用 |
| 导入 | 有 | 有 | 有 | 有 | 有 | 有 | 可用 |
| 监听文件夹 | 有 | 有 | 有 | 有 | 有 | 有 | 可用 |
| 未解析 | 有 | 有 | 有 | 有 | 有 | 有 | 可浏览不可提交 |
| AI 任务 | 有 | 有 | 有 | 有 | 有 | 有 | 不可提交 |
| 关键词管理 | 有 | 有 | 有 | 有 | 有 | 有 | 可用 |
| 搜索 | 有 | 有 | 有 | 有 | 不适用 | 有 | 降级 |
| 设置 | 有 | 不适用 | 有 | 有 | 有 | 有 | 部分可用 |
| 插件 | 有 | 不适用 | 有 | 有 | 有 | 有 | 部分可用 |

## 18. 跨页面状态一致性

### 18.1 图片新增

图片保存或导入成功后，需要同步更新：

- 全局素材库
- 当前两级项目文件夹
- 未解析数量
- 最近图片
- 图片总数
- 搜索索引

### 18.2 关键词修改

关键词确认、拒绝、增加、删除或重命名后，需要同步更新：

- 图片详情
- 图片卡片
- 左侧关键词栏的关联数量和可见性
- 搜索索引
- 关键词管理页关联数量
- 未解析状态和数量

### 18.3 当前项目类型或项目名称修改

项目类型切换或项目名称修改后，需要同步更新：

- 插件主面板
- 网页采集覆盖提示
- 两级文件夹路径
- 桌面应用最近项目
- 左侧项目树和图片数量

### 18.4 网络状态修改

断网后：

- 已打开页面保持可用。
- AI 提交入口禁用并说明原因。
- 自然语言搜索降级。
- 本地编辑继续保存。

恢复网络后：

- 不自动提交用户此前未确认的 AI 任务。
- 可以重试失败任务。
- 查询可以由用户主动恢复混合搜索。

## 19. 页面级权限与安全边界

- 插件只在用户主动开启图片保存功能后处理工具条中的保存操作。
- 插件权限不足时说明缺失能力，不诱导用户授予无关权限。
- 桌面应用内部核心模块写入前验证目标目录。
- 产品不提供永久删除磁盘原图的入口。
- 图片上传 AI 前必须由用户主动提交分析。
- 行为统计授权与核心服务授权分开。
- 来源页面打开前使用原始记录 URL，不拼接不明脚本。

## 20. 原型测试任务

### 任务 1：首次激活

目标：用户完成注册、环境检查并进入素材库。

观察：

- 是否理解桌面应用与插件的关系
- 是否理解详细统计授权
- 是否在无帮助情况下完成连接

### 任务 2：采集 5 张图片

目标：选择项目类型，从重点网站保存 5 张图片，并确认系统按网页标题自动建立项目文件夹。

观察：

- 是否找到项目类型选择
- 是否理解项目名称来自网页标题且可以修改
- 是否理解图片保存功能的开启与关闭状态
- 是否误触网页原有功能
- 是否理解保存成功、重复和失败反馈

### 任务 3：AI 分析与校正

目标：从未解析栏分析 5 张图片，查看描述，确认正确关键词并拒绝错误关键词。

观察：

- 是否理解服务可用且额度充足时 AI 会自动运行，服务不可用时图片会留在队列
- 是否理解额度和任务状态
- 是否理解解析成功必须包含描述和关键词
- 是否能区分 AI 生成与人工修改的关键词标签

### 任务 4：校正 AI 新关键词

目标：查看一个 AI 自主新增的词库外关键词，确认其已进入“其他”和个人词库，将其移动到正确分类，并为多张图片批量添加。

观察：

- 是否理解 AI 会优先选择预设、只在必要时新增少量标签
- 是否能删除不准确的新词，或将准确的新词移动到正确分类
- 是否理解 AI 新增词会参与后续 AI 选择和分类筛选

### 任务 5：双路径浏览

目标：分别通过项目树和关键词栏找到图片，再组合“文化建筑＋混凝土”缩小结果。

观察：

- 是否理解项目树对应实际文件夹。
- 是否理解关键词栏来自 AI 和人工确认结果。
- 是否能发现项目与关键词可以组合。
- 是否能看见并分别清除当前筛选条件。

### 任务 6：自然语言找图

目标：用一句自然语言，在两次查询内找到一张指定或满意图片。

观察：

- 是否理解统一搜索框
- 是否注意并使用查询理解条件
- 是否理解匹配原因
- 无结果时能否恢复

### 任务 7：复用与来源

目标：把结果复制到另一个项目、复制图片并打开来源。

观察：

- 是否理解跨项目会生成独立文件副本
- 是否能顺利回到搜索结果

## 21. UX 验收标准

### 21.1 核心流程

- 新用户无需外部指导即可完成“注册 → 选择项目类型 → 开启图片保存功能”。
- 项目名称可从网页标题自动生成，用户无需提前输入。
- 当前项目类型设置后，单张图片采集仅需一次点击。
- 图片保存功能的开启和关闭状态始终明确。
- 用户可以从任何 AI 失败状态找到重试路径。
- 用户可以识别 AI 生成、用户创建和用户修改的关键词标签。
- 用户可以分别使用项目树、关键词栏和顶部自然语言搜索，并理解三者可以组合。
- 搜索结果页能够解释系统如何理解查询。
- 所有无结果和错误状态都有明确下一步。

### 21.2 状态完整性

- P0 页面均具备加载、空、错误状态。
- 涉及网络的页面均具备离线或降级状态。
- 涉及批量操作的页面均显示影响数量。
- 涉及本地文件的危险操作均明确说明是否删除磁盘文件。

### 21.3 连续性

- 从列表进入详情再返回时保留上下文。
- 断网不会丢失本地人工编辑。
- AI 批次部分失败不会丢失成功结果。
- 重复图片可以关联新项目，而无需生成副本。

## 22. 低保真线框图输入清单

下一阶段需要为以下页面制作线框图：

| 顺序 | 页面 ID | 页面 |
| --- | --- | --- |
| 1 | EXT-01 | 插件主面板 |
| 2 | EXT-02 | 网页采集覆盖状态 |
| 3 | ONB-01～05 | 首次启动与环境检查 |
| 4 | SRCH-01 | 首页与统一搜索 |
| 5 | LIB-TYPE-01 | 项目类型选择 |
| 6 | LIB-PRJ-01 | 网页项目名称与文件夹预览 |
| 7 | PARSE-01 | 未解析 |
| 8 | ASSET-01 | 图片详情 |
| 9 | AI-01 | AI 分析确认 |
| 10 | AI-02 | AI 任务中心 |
| 11 | TAG-01 | 批量关键词编辑 |
| 12 | TAG-02 | 关键词管理 |
| 13 | IMP-01 | 文件导入 |
| 14 | IMP-02 | 监听文件夹 |
| 15 | ASSET-02 | 移到回收站确认 |
| 16 | SET-01～04 | 设置与连接诊断 |

优先制作并测试的最小页面集合：

1. EXT-01 插件主面板
2. EXT-02 网页采集覆盖状态
3. SRCH-01 首页与搜索
4. PARSE-01 未解析
5. ASSET-01 图片详情
6. AI-01 AI 分析确认
7. AI-02 AI 任务中心
8. TAG-01 批量关键词编辑
